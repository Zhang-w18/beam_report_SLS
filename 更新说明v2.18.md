# 更新说明 v2.18：每 TRP 功率语义、正式 TTI KPI 与局部 NACK 曲线

## 1. 更新目的与原因

本轮更新解决两个问题：

1. 统一 YAML 发射功率的物理含义；
2. 补充 warm-up 后的方案级链路统计和小区 0 的 UE 局部 NACK rate 演化曲线。

旧实现把：

```yaml
system:
  tx_power_dbm: 33.0
```

先转换为线性功率，再除以：

```text
全网小区数 × 每小区并行发射资源数
```

因此，相同 TRP 配置在增加站点或小区后，每个物理面板的功率会被错误降低。
部分示例 YAML 为了补偿该行为，把 9 个 TRP 的功率写成 `42.542 dBm`、把
21 个 TRP 的功率写成 `46.222 dBm`，这实际上把参数当成了全网总功率。

v2.18 将 `system.tx_power_dbm` 固定解释为：

```text
每一个 TRP 的总发射功率
```

每个 TRP 独立获得该功率预算，然后在线性功率域均分给该 TRP 的所有物理面板。

旧 summary 已包含平均 TBLER 和平均 effective SINR，但没有直接给出：

- TBLER 精确等于 0 的比例；
- 每个正式 TTI 的平均调度用户数；
- 调度后真实 effective SINR 分布的 5%、50%、95% 分位值；
- 每个 drop 中，小区 0 关联 UE 的局部 NACK rate 随已调度样本数的变化。

本轮补充这些输出，并统一规定它们只统计 warm-up 之后的正式 TTI。

## 2. 每 TRP 总功率与面板均分

### 2.1 新功率公式

YAML 中：

```yaml
system:
  tx_power_dbm: P_trp_dbm
```

表示每个 TRP 的总功率。代码按以下方式解析：

```text
P_trp_watt
  = 10 ^ ((P_trp_dbm - 30) / 10)

P_panel_watt
  = P_trp_watt / num_physical_panels_per_trp
```

功率只除以单个 TRP 的物理面板数，不再除以：

- 站点数；
- 小区数；
- 网络 TRP 总数；
- TXRU 数；
- 调度域中的并行 beam 数。

例如：

```yaml
system:
  tx_power_dbm: 33.0
```

一个 TRP 有 2 个物理面板时：

```text
TRP 总功率    ≈ 1.995 W = 33.000 dBm
每面板功率    ≈ 0.998 W = 29.990 dBm
```

不论网络中有 1 个还是 21 个 TRP，每个 TRP 都使用相同的 33 dBm 总功率预算。

### 2.2 `panel_power_mode` 兼容行为

旧字段：

```yaml
trp:
  panel_power_mode: per_tx_unit_equal
```

已弃用。v2.18 会忽略该字段，任何旧取值都不能覆盖“每 TRP 总功率、均分到物理
面板”的固定语义。

仓库自带 YAML 已删除该字段。仍带有该字段的外部旧 YAML 可以继续加载，但建议
删除，以免产生误解。

### 2.3 示例 YAML 功率值调整

原先用全网总功率补偿旧分配逻辑的配置已改回：

```yaml
system:
  tx_power_dbm: 33.0
```

涉及：

- `configs/v2_12_1site3cell_persite_baseline_topk_50drop_200tti_10ue.yaml`；
- `configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml`；
- `configs/v2_7_three_site_global_18ue_distance_range_v4_5ue_eval_matrix.yaml`；
- `configs/v2_three_site_global_36ue.yaml`；
- `configs/v2_three_site_global_36ue_fixed_v8_5ue_eval_matrix.yaml`；
- `configs/v_2_10_2TXRU_3sites_global.yaml`。

解析后的功率会写入：

```text
resolved_config.yaml
array_config_summary.json
metrics/drops.csv
```

主要字段为：

```text
tx_power_semantics = total_per_trp_equal_physical_panels
tx_power_w_per_trp
tx_power_w_per_panel
num_physical_panels_per_trp
tx_power_per_trp_dbm
tx_power_per_panel_dbm
```

`metrics/drops.csv` 暂时保留旧字段 `tx_power_per_tx_unit_dbm`，作为兼容别名；
其数值与新的 `tx_power_per_panel_dbm` 相同，新分析脚本应优先读取新字段。

## 3. 方案级 warm-up 后 KPI

### 3.1 统一统计范围

`metrics/link_tti.csv` 只记录正式 TTI，不记录负 TTI 编号的 warm-up 样本。
v2.18 的新增 KPI 全部从该正式样本集合和完整正式 TTI 网格构造，因此：

```text
warm-up TTI 不进入 TBLER、SINR、调度用户数或局部 NACK 统计
```

每个 evaluation case 使用 `case_id` 作为 summary 中的 `scheme`。如果启用了：

```yaml
analysis:
  baseline_no_interference_upper_bound: true
```

诊断上界也会采用相同口径生成 KPI。

### 3.2 `tbler_zero_ratio`

定义为：

```text
正式 TTI 中 TBLER 精确等于 0 的已调度链路样本数
---------------------------------------------------
正式 TTI 中全部已调度链路样本数
```

该指标按已调度链路样本统计，不把未调度 UE 或零调度 TTI补成 TBLER=0。
如果方案没有已调度链路样本，结果为 `0.0`。

### 3.3 `avg_scheduled_users_per_tti`

先对每个：

```text
(scheme, drop, formal TTI)
```

统计唯一 `ue_id` 数，再在全部正式 TTI 上求平均。分母包含零调度 TTI，因此不会
只对有调度结果的 TTI 求平均。

### 3.4 调度后真实 effective SINR 分位值

新增：

```text
p05_effective_sinr_db
p50_effective_sinr_db
p95_effective_sinr_db
```

数据源为合并全网 schedule 后，使用真实信道、真实共调度干扰和 EESM 得到的：

```text
metrics/link_tti.csv::effective_sinr_db
```

因此它们表示已调度链路样本 CDF 的 5%、50%、95% 分位值，不是调度器预测的
`predicted_sinr_db` 分位值。方案没有样本时三个值均为 `0.0`。

### 3.5 控制台与 summary 输出

运行结束时控制台按方案打印：

```text
[summary] post-warmup scheme KPIs
[summary][<scheme>] tbler_zero_ratio=...,
  avg_scheduled_users_per_tti=...,
  effective_sinr_db[p05,p50,p95]=[..., ..., ...]
```

相同数值写入：

```text
metrics/summary.json
metrics/summary.csv
```

每个方案新增字段：

```text
tbler_zero_ratio
avg_scheduled_users_per_tti
p05_effective_sinr_db
p50_effective_sinr_db
p95_effective_sinr_db
```

原有 `avg_tbler`、`avg_eff_sinr_db`、`ack_rate` 等字段继续保留。

## 4. 小区 0 每 UE 局部 NACK rate

### 4.1 UE 范围

每个 drop 完成 average-RSRP 关联后，从 `metrics/ues.csv` 选择：

```text
serving_cell == 0
```

的全部 UE。统计按以下键完全隔离：

```text
(scheme, drop, ue_id)
```

不同方案、drop 或 UE 的 ACK/NACK 样本不会混合。

### 4.2 500 个“被调度 TTI”窗口

对每个 UE，只保留该 UE 实际被调度的正式 TTI，并按仿真 TTI 顺序排列：

```text
该 UE 第 1 次被调度
该 UE 第 2 次被调度
...
```

然后每 500 个样本划分一个窗口：

```text
窗口 1：第    1～ 500 次被调度
窗口 2：第  501～1000 次被调度
窗口 3：第 1001～1500 次被调度
...
```

窗口内 500 个 TTI 都调度了该 UE。这里不是按连续仿真 TTI `[0, 499]`、
`[500, 999]` 切分；UE 未被调度的 TTI 不推进窗口计数。

每个完整窗口计算：

```text
local_nack_rate = num_nack / 500
num_nack        = sum(1 - ack)
```

不足 500 个已调度样本的尾段不生成曲线点，以保证每个点的分母严格相同。

### 4.3 `metrics/cell0_local_nack_rate.csv`

粒度为：

```text
(scheme, drop, cell_id=0, ue_id, complete 500-sample window)
```

字段包括：

```text
scheme
drop
cell_id
ue_id
window_index
scheduled_tti_index_start
scheduled_tti_index_end
simulation_tti_start
simulation_tti_end
num_scheduled_tti
num_nack
local_nack_rate
```

其中：

- `scheduled_tti_index_*` 是该 UE 的累计被调度样本序号；
- `simulation_tti_*` 是该窗口首尾样本对应的原始正式 TTI 编号；
- `num_scheduled_tti` 对有效曲线点固定为 `500`。

如果整个仿真没有任何完整窗口，该 CSV 当前为空；应结合 status CSV 和说明性图片
判断原因。

### 4.4 `metrics/cell0_local_nack_rate_status.csv`

粒度为：

```text
(scheme, drop, cell_id=0, associated ue_id)
```

该文件覆盖所有关联到小区 0 的 UE，包括从未被调度或不足 500 次的 UE。字段包括：

```text
window_size_scheduled_tti
num_scheduled_tti
num_complete_windows
discarded_tail_scheduled_tti
tti_scope = post_warmup_only
```

例如：

```text
num_scheduled_tti              = 1201
num_complete_windows           = 2
discarded_tail_scheduled_tti   = 201
```

表示前 1000 个已调度样本生成两个曲线点，最后 201 个样本不参与曲线。

### 4.5 局部 NACK rate 图片

每个方案、每个 drop 生成：

```text
figures/cell0_local_nack_rate/<scheme>/drop_<drop>.png
```

横轴为：

```text
该 UE 累计被调度 TTI 数：500、1000、1500、...
```

纵轴为：

```text
当前 500 样本窗口内的 local NACK rate
```

同一图片中，每个关联 UE 使用一条独立曲线。某个 drop 没有完整 500 样本窗口时，
仍会生成说明性图片，并显示：

```text
No complete 500-scheduled-TTI window
```

`metrics/summary.json` 的 `_cell0_local_nack_rate` 节点记录窗口大小、统计范围、
完整窗口总数以及上述 CSV/图片目录。该元数据节点不写入扁平的 `summary.csv`。

## 5. 更新后的 YAML 使用方式

### 5.1 功率和 TTI 配置示例

```yaml
system:
  # 每一个 TRP 的总发射功率。
  tx_power_dbm: 33.0

  continuous_tti:
    enabled: true
    # 为局部 NACK 曲线保留足够多的正式 TTI。
    num_tti: 5000
    warmup_tti: 50

trp:
  num_trps_per_sector: 1
  # 不再配置 panel_power_mode。
```

方案级 KPI 和局部 NACK 输出默认生成，不需要新增开关。

`num_tti: 5000` 只是示例，不保证每个 UE 都能累积 500 次调度。所需正式 TTI 数
取决于 UE 数量、调度算法、PF 状态、outage 和资源规模。应通过：

```text
metrics/cell0_local_nack_rate_status.csv
```

检查每个 UE 的 `num_scheduled_tti` 和 `num_complete_windows`。

### 5.2 常规完整运行

```bash
python -m beam_sls.run \
  --config configs/v2_12_1site3cell_persite_baseline_topk_50drop_200tti_10ue.yaml \
  --out runs/v2_18_1site3cell
```

该配置默认只有 200 个正式 TTI/drop，因此大多数 UE 不会形成完整的 500 次被调度
窗口。方案 KPI、status CSV 和说明性图片仍会正常生成。

### 5.3 局部 NACK 长时运行示例

```bash
python -m beam_sls.run \
  --config configs/v2_12_1site3cell_persite_baseline_topk_50drop_200tti_10ue.yaml \
  --out runs/v2_18_cell0_local_nack \
  --num-drops 1 \
  --num-tti 5000 \
  --warmup-tti 50 \
  --skip-heatmap
```

### 5.4 建议先运行缩小规模 smoke

```bash
python -m beam_sls.run \
  --config configs/v2_one_site_three_sector.yaml \
  --out runs/v2_18_smoke \
  --num-drops 1 \
  --num-tti 2 \
  --warmup-tti 1 \
  --skip-heatmap
```

smoke 运行不会产生完整 500 样本窗口，但可检查：

```text
控制台方案 KPI
metrics/summary.csv
metrics/cell0_local_nack_rate_status.csv
figures/cell0_local_nack_rate/<scheme>/drop_0000.png
```

## 6. 主要修改文件

- `beam_sls/sim.py`
  - 新增每 TRP 总功率到每物理面板功率的解析；
  - 在 resolved config、array summary 和 drop 指标中记录功率语义；
  - 新增方案级正式 TTI KPI；
  - 新增小区 0 局部 NACK rate 窗口聚合；
  - 输出控制台 summary、CSV、JSON 元数据并调用绘图。
- `beam_sls/plotting.py`
  - 新增每方案、每 drop 的多 UE 局部 NACK rate 曲线。
- `beam_sls/config.py`
  - 明确 `tx_power_dbm` 的每 TRP 总功率含义；
  - 删除 `panel_power_mode` 默认项。
- `configs/*.yaml`
  - 删除 `panel_power_mode`；
  - 将旧的全网总功率补偿值改为 `33.0 dBm/TRP`。
- `docs/yaml_parameter_reference.md`
  - 更新功率参数、弃用字段和面板均分说明。
- `docs/repository_memory.md`
  - 更新稳定功率语义和新增输出文件。
- `README.md`
  - 增加 summary 字段、局部 NACK 输出和解释。
- `tests/test_smoke.py`
  - 增加功率只按物理面板均分的回归；
  - 增加方案 KPI 分母和分位值回归；
  - 增加完整 500 次被调度 TTI 窗口回归；
  - 端到端检查新增 CSV 和图片。

## 7. 验证结果

已完成：

```text
python -m compileall -q beam_sls tests
git diff --check
```

结果通过。

使用隔离的临时测试依赖执行完整测试套件：

```text
83 passed
```

其中包括：

- 功率不随站点、小区和 TRP 数再次分摊；
- 2-panel TRP 的 40 dBm 总功率得到 5 W/panel；
- `tbler_zero_ratio` 使用已调度正式链路样本；
- 平均调度用户数包含零调度正式 TTI；
- effective SINR P05/P50/P95 与 NumPy percentile 对齐；
- 局部 NACK 只输出完整 500 样本窗口；
- 非小区 0 关联 UE 不进入 cell-0 输出；
- 不足 500 的尾段只进入 status，不进入曲线；
- 连续 TTI 和 warm-up 回归；
- 端到端生成 summary、局部 NACK CSV 和图片。

仓库内 13 个 YAML 配置均已通过 UTF-8 加载和 YAML 解析。

当前验证环境没有安装完整 Sionna/TensorFlow 正式运行后端；测试套件中的相关分支
按项目现有兼容逻辑执行或跳过。应在服务器正式环境补充一次代表性 Sionna smoke，
重点确认启动日志中的每 TRP/每面板功率以及长时运行的 NACK 曲线点数。

## 8. 兼容性与已知限制

- `system.tx_power_dbm` 的默认值仍为 `33.0`，但物理语义从旧的全网分摊改为
  每 TRP 总功率；多站点运行的接收功率和 SINR 会因此发生预期变化；
- 外部旧 YAML 中的 `trp.panel_power_mode` 可以继续存在，但会被忽略；
- 旧示例配置中的 `37.782/42.542/46.222 dBm` 已改为 `33.0 dBm/TRP`，避免在
  新语义下产生过高功率；
- `metrics/drops.csv::tx_power_per_tx_unit_dbm` 仅为兼容别名，新脚本应读取
  `tx_power_per_panel_dbm`；
- 新增 KPI 只使用 warm-up 后正式 TTI；已有吞吐、ACK、OLLA 和 PF 更新规则不变；
- `tbler_zero_ratio` 使用浮点值与 `0.0` 的精确相等判断，不使用容差；
- effective SINR 分位值按已调度链路样本加权，调度频率更高的 UE 会贡献更多样本；
- 平均调度用户数包含零调度 TTI，不能与只对非空 schedule 求平均的旧外部脚本
  直接比较；
- 局部 NACK 窗口按每个 UE 的被调度样本序列划分，不是固定仿真 TTI 区间；
- 不完整尾段不参与 NACK 曲线，因此正式 TTI 太少时只会生成 status 和说明性图片；
- 每个方案、每个 drop 都生成一张局部 NACK 图片；大量 drop/case 会增加绘图时间和
  输出文件数量；
- 局部 NACK rate 使用实际 ACK/NACK 抽样结果 `1 - ack`，不是 TBLER 的窗口平均；
- 本轮未新增 YAML 开关，方案 KPI、CSV 和局部 NACK 图片默认生成。

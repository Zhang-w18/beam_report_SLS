# 更新说明 v2.21：3GPP 阵列、全网 UE 撒点与中心站统计

## 1. 更新目的与原因

v2.21 统一修正天线阵列、UE 拓扑和 7 站点统计口径。

旧实现把 `Mp/Np` 当成额外的面板重复维度，因此将它们错误地乘进物理阵元数、
Sionna `PanelArray` 几何和物理面板数。这与目标 3GPP 参数含义不一致，也会在改变
`Mp/Np` 后生成错误的信道天线维度。

旧 UE drop 按每个扇区分别撒点并用几何最近站/扇区初始化关联，无法表达“全网区域
统一撒点后按长期接收功率选择服务 TRP”的流程；同时所有 UE 都作为 outdoor 传给
Sionna，不能控制室内/室外比例。

7 站点仿真中，外围站主要用于提供真实干扰环境。最终 KPI 需要只统计中心站 3 个
扇区，并通过更长的连续 TTI 窗口弥补有效样本量下降。

## 2. 更新方式

### 2.1 3GPP 七参数物理定义

`beam_sls/codebook.py` 和 `beam_sls/rf.py` 现在按以下定义解析：

| 参数 | v2.21 物理含义 |
|---|---|
| `M` | 每面板、每列、每种极化的垂直阵元数 |
| `N` | 每面板水平方向的阵元列数 |
| `P` | 每个天线位置的极化数，常用 1 或 2 |
| `Mg` | 垂直方向物理面板数 |
| `Ng` | 水平方向物理面板数 |
| `Mp` | 每面板、每种极化内垂直 TXRU 数 |
| `Np` | 每面板、每种极化内水平 TXRU 数 |

核心关系为：

```text
num_AE = M * N * P * Mg * Ng
num_physical_TXRU = Mg * Ng * Mp * Np * P
num_spatial_TXRU_groups = Mg * Ng * Mp * Np
TXRU subarray size = (M/Mp) * (N/Np) AEs per polarization
```

程序要求 `M` 可被 `Mp` 整除、`N` 可被 `Np` 整除，并校验显式 `num_ae`。
`Mp/Np` 不再进入 Sionna `PanelArray` 的物理面板或阵元维度。

在 `allow_independent_polarization_beams: false` 时，同一空间位置的不同极化 TXRU
使用相同波束，因此：

```text
max_parallel_beams_per_trp = Mg * Ng * Mp * Np
```

默认 `(8,16,2,1,1;1,1)` 解析为 256 AEs、2 个物理 TXRU、1 个并发空间波束。
码本完整尺寸为 `16*8=128`，默认 YAML 对每个 TRP 扫描全部 128 个 TX beam。

不相交划分也覆盖两端：`Mp=Np=1` 是大子阵部分连接；`Mp=M,Np=N` 是每阵元、
每极化一个 TXRU 的全数字极限。全数字极限自动使用 joint full-array DFT 码本，
并发空间流上限为 `Mg*Ng*M*N`。

### 2.2 室内/室外 UE 与 Sionna

`beam_sls/topology.py` 新增 `UE.is_indoor` 和：

```yaml
ue_drop:
  indoor_probability: 0.4
```

每个 drop 随机选择 UE，并保证室内 UE 数为
`round(indoor_probability*num_ues)`。`beam_sls/channel.py` 将该布尔数组传给
Sionna TR 38.901：

```text
channel_model.set_topology(..., in_state=indoor_states)
```

因此默认是 40% indoor、60% outdoor，UMa 的 O2I 行为由 Sionna 和
`scenario.o2i_model` 决定。`metrics/ues.csv` 新增 `is_indoor` 字段。

### 2.3 全区域统一撒点与 RSRP 关联

默认模式改为：

```yaml
ue_drop:
  num_ut_per_sector: 10
  distribution: uniform_in_network
  network_hex_radius_m: null
```

处理流程：

1. 总 UE 数固定为 `num_cells*num_ut_per_sector`；
2. 在所有站点等面积 Voronoi 六边形的并集内统一均匀撒点；
3. `network_hex_radius_m: null` 时六边形外接圆半径为 `ISD/sqrt(3)`；
4. 任一候选点只要与任一 BS 的 2D 距离小于
   `scenario.min_ue_distance_m` 就被拒绝；
5. 信道生成后，每个 TRP 遍历完整 TX 码本，并联合所有 UE RX beam；
6. 对每个波束计算频点线性功率平均，取该 TRP 最强 RSRP；
7. 全网最强 RSRP 波束所属 TRP 成为 UE 的最终 serving cell。

旧 `uniform_in_sector` 仍可显式配置，用于复现旧 YAML；无论采用哪种撒点模式，
主仿真的最终关联都由 beam-swept average RSRP 决定。

### 2.4 7 站点中心站统计

新增：

```yaml
statistics:
  cell_scope: center_site_for_seven_site
  center_site_id: 0
```

7 站点时，聚合 KPI 只使用中心站 cells `[0,1,2]` 的最终关联 UE。外围 18 个扇区
仍参与信道生成、波束扫描、关联、反馈、调度和实际干扰计算，不是三小区孤立仿真。

为兼顾可追溯性：

- `metrics/link_tti.csv`、`metrics/ues.csv` 保留全网原始明细；
- 两个文件新增/包含 `statistics_in_scope`；
- `metrics/ue_goodput.csv`、`metrics/system_tti_goodput.csv`、summary、CDF、
  SU-SNR 和 scheduled-SU 汇总只使用统计范围内 UE；
- 新增根目录 `statistics_scope.json`，记录解析后的 site/cell 范围；
- `metrics/drops.csv` 新增 `num_statistics_ues`、`statistics_cell_ids`、
  `num_spatial_txru_groups_per_trp`。

### 2.5 v2.21 默认无线和时间配置

新增 `configs/v2_21_default.yaml`：

```yaml
scenario:
  carrier_frequency_ghz: 30.0
  channel_model: sionna_tr38901_uma
  min_ue_distance_m: 35.0

topology:
  layout: seven_site_hex
  num_sites: 7
  sectors_per_site: 3
  isd_m: 500.0

system:
  subcarrier_spacing_khz: 120.0
  tx_power_dbm: 33.0
  num_drops: 1
  continuous_tti:
    enabled: true
    duration_ms: 2500.0
    num_tti: 20000
    warmup_tti: 5000

pdsch:
  num_prbs: 66
  slot_duration_ms: 0.125

ue_drop:
  distribution: uniform_in_network
  indoor_probability: 0.4
```

100 MHz、120 kHz SCS 在 NR FR2 中使用 66 PRB；平台用于频域采样和热噪声积分的
实际占用带宽为：

```text
66 * 12 * 120 kHz = 95.04 MHz
```

`num_tti=20000` 优先于 `duration_ms`，正式统计 20000 TTI；前 5000 TTI 推进
信道、PF、调度和 OLLA，但不写入正式吞吐、BLER 或 CDF 统计。

版本标识已更新为 `0.2.21`。
`python -m beam_sls.run` 的默认配置和默认输出目录也分别改为
`configs/v2_21_default.yaml` 与 `runs/v2_21_default`。

## 3. 更新后如何使用

### 3.1 完整默认仿真

```bash
python -m beam_sls.run \
  --config configs/v2_21_default.yaml \
  --out runs/v2_21_default
```

该配置包含 210 UE、21 TRP、每 TRP 128 个 TX beam，以及 5000+20000 个连续
TTI，运行量较大。建议先做缩小配置验证环境，再提交正式任务。

### 3.2 用 CLI 缩短 TTI 的检查运行

```bash
python -m beam_sls.run \
  --config configs/v2_21_default.yaml \
  --out runs/v2_21_short_tti \
  --num-drops 1 \
  --num-tti 2 \
  --warmup-tti 1 \
  --skip-heatmap
```

CLI 只缩短 drop/TTI，不减少 UE、beam 或频点。如果还需快速检查 Sionna/GPU，复制
默认 YAML 后临时减小 `ue_drop.num_ut_per_sector`、`tx_array.num_beams_h/v` 和
`measurement.num_freq_points`。

### 3.3 调整 indoor/outdoor 比例

```yaml
ue_drop:
  indoor_probability: 0.25  # 25% indoor, 75% outdoor
```

### 3.4 统计全部 21 个小区

```yaml
statistics:
  cell_scope: all
```

也可显式指定：

```yaml
statistics:
  cell_ids: [0, 1, 2]
```

## 4. 输出文件与解读

### 4.1 新增输出

`statistics_scope.json`：解析后的统计 site/cell 范围，以及“原始明细保留全网、聚合
KPI 使用所选范围”的标志。

### 4.2 变化字段

- `metrics/ues.csv`：`is_indoor`、`statistics_in_scope`；
- `metrics/link_tti.csv`：`statistics_in_scope`；
- `metrics/drops.csv`：`num_statistics_ues`、`statistics_cell_ids`、
  `num_physical_panels_per_trp`、`num_spatial_txru_groups_per_trp`；
- `array_config_summary.json`：`derived_num_txru`、`num_physical_panels`、
  `num_spatial_txru_groups`、TXRU 子阵水平/垂直尺寸；
- `rf_architecture_summary.json`：`fully_digital_partition` 和新并发波束解析结果。

`tx_power_per_panel_dbm` 是为兼容旧后处理保留的字段名；v2.21 中其值表示每个空间
TXRU 组的功率。

## 5. 验证与兼容性

已执行：

```text
python -m pytest -q --tb=short --basetemp=.pytest_tmp_v221
99 passed
```

增加的测试覆盖：

- `Mp/Np` 只划分 TXRU、不增加 AE；
- `Mp=M,Np=N` 全数字极限；
- 默认 256-AE/2-TXRU/1-beam 解析；
- 66 PRB 对应 95.04 MHz 实际占用带宽；
- 全网 UE 总数、所有 BS 35 m 排除区和 40% indoor；
- 7 站点中心 cells `[0,1,2]` 统计范围；
- 连续 TTI、调度、链路和原有功能回归。

还使用已安装 Sionna UMa 做了小规模真实后端检查，得到：

```text
H shape = (3 UE, 3 TRP, 2 freq, 16 RX-AE, 256 TX-AE)
indoor UE = 1/3
backend = sionna_tr38901_uma
```

并运行了缩小版 7 站点端到端仿真（21 UE、4 TX beams/TRP、1 warmup + 2 正式
TTI），确认 RSRP 关联、中心站统计和全部输出链路可完成。

兼容性说明：

- `uniform_in_sector` 保留；
- `num_array_panels`、`tx_panel_index`、`tx_power_per_panel_dbm` 等旧内部/输出名称
  保留，但 v2.21 在 sub-connected 模式下解释为空间 TXRU 子阵/组；
- sub-connected 模式以七个阵列参数为权威，旧 YAML 中冲突的
  `rf_architecture.num_txru` 不再覆盖推导值；
- `fully_connected` 显式模式继续支持；
- 正式 5000+20000 TTI 默认任务未在本次更新中完整跑完，只做了短 TTI 流程验证。

# 更新说明 v2.19：候选 UE Beam Sharing 与窗口 Active UE Beam Collision 统计

## 1. 需求澄清、修改原因

本次重新定义并拆分统计口径，避免把“一个 drop 内候选 UE 的静态 beam sharing”和
“一个 observation window 内发生业务的 active UE collision”混为同一个指标。

固定一个 radio drop 后，先生成候选 UE、信道和每个 UE 的 best/service beam。假设一个
drop 有 210 个候选 UE，则静态统计回答：

```text
这 210 个 UE 中，有多少 UE 的 best/service beam 是 beam b？
```

随后在每个长度为 `T_obs` 的窗口内，对每个固定 UE 独立产生 FTP arrival。窗口内发生
至少一次 arrival 的 UE 定义为 active UE，按该 UE 已缓存的 best/service beam 分组，
统计回答：

```text
这个窗口内，有多少 active UE 会上报同一个服务 beam？
```

窗口结束后清空 active 状态，下一窗口重新独立统计。不模拟文件传输完成时间、服务
时间、队列、积压或调度吞吐。

## 2. 修改内容

### 2.1 Drop 级固定 UE 与服务波束缓存

`beam_sls/service_beam_statistics.py` 对每个 drop 执行：

```text
make_topology() 固定候选 UE 和位置
  → 生成信道
  → 按宽带平均 RSRP 完成 serving cell 关联
  → 在 serving cell 的可用 TX beam 中选择 service_power_w 最大者
  → 功率相同取最小全局 beam_index
  → 按 ue_id 缓存 best service beam、best RX beam 和 service power
```

同一个 drop 内，UE 位置、信道和 best beam 只计算一次。不同 observation window 再次
出现同一个 `ue_id` 时，只查询缓存，不重新计算信道或 beam。

缓存文件为 `metrics/service_beam_ue_cache.csv`，每个 `(drop, ue_id)` 一行，字段为：

```text
drop, ue_id, x_m, y_m, serving_cell,
best_service_beam_index, best_service_beam_id,
best_rx_beam_index, service_power_w
```

### 2.2 两类 beam UE 数统计

新增 `beam_sls/ftp_traffic.py::count_ues_by_beam()`，统计一个 drop 中所有候选 UE 的
静态 beam sharing 数：

```text
C[b, drop] = sum_u 1{best_service_beam[u] == b}
```

新增窗口 active UE 统计：

```text
X[u,t] ~ Poisson(arrival_rate_per_ue_s × T_obs)
A[u,t] = 1{X[u,t] > 0}
N[b,t] = sum_u A[u,t] × 1{best_service_beam[u] == b}
```

`N[b,t]` 是本次需求中的窗口 beam collision 数。同一 UE 在同一窗口内即使
`num_arrivals > 1`，也只计为一个 active UE。窗口结束时不保留任何业务状态。

统计主流程不再使用匿名 cell 级 arrival 总数，也不使用 beam selection probability 做
multinomial 分配。业务 arrival 始终通过 `ue_id` 查询服务波束缓存。

### 2.3 随机流与独立运行模式

`system.run_mode: service_beam_statistics` 采用独立入口：

```text
stream 0: topology + channel
stream 1: per-UE FTP arrival
```

固定 `random_seed` 时，改变 `observations_per_drop` 不会改变同一 drop 的 UE、信道和
best beam。统计模式不会初始化或调用：

```text
feedback
evaluation matrix
scheduler
link adaptation
OLLA
ACK/NACK
TBLER
throughput
run_tti_loop
```

默认 `system.run_mode` 仍为 `scheduling`，原有调度流程不改变。

## 3. 修改后如何使用

### 3.1 YAML 配置

根据文件夹中的 `v2.19 yaml.txt` 整理生成默认配置：

```text
configs/v2_19_default.yaml
```

该默认 YAML 使用七站点、21 小区、每小区 10 个候选 UE，即每个 drop 共 210 个候选
UE；运行模式已经设置为 `service_beam_statistics`。原始 TXT 中的解释性文字已转换为
YAML 注释或删除，避免被 YAML 解析器误认为配置内容。

```yaml
system:
  run_mode: service_beam_statistics
  num_drops: 10
  random_seed: 20260610

service_beam_statistics:
  observations_per_drop: 10000
  observation_interval_ms: 100.0
  candidate_ues_per_sector_per_drop: 70  # 3 sectors × 70 = 210 candidates
  include_zero_count: true
  warmup_observations: 0
  save_per_ue_arrival_samples: false
  traffic:
    model: 3gpp_ftp_model_3_arrival_only
    arrival_rate_per_ue_s: 0.2
    file_size_mbytes: 0.5
```

说明：

- `candidate_ues_per_sector_per_drop` 控制每个 sector 的固定候选 UE 数；上例在 3 个
  sector 时为 210 个 UE。设为 `null` 时复用 `ue_drop.num_ut_per_sector`；
- `observations_per_drop` 只影响 active UE 窗口样本数，不影响 candidate beam sharing；
- `observation_interval_ms` 是窗口长度 (T_{m obs})；
- `arrival_rate_per_ue_s` 是每个 UE 独立的 arrival rate；
- `file_size_mbytes` 只记录业务配置，不参与 beam collision 计数；
- `save_per_ue_arrival_samples: true` 时保存所有 UE 的窗口 arrival 记录，适合验收；
  `false` 时只保存有 arrival 的 UE。

### 3.2 默认 YAML 运行命令

```bash
python -m beam_sls.run \
  --config configs/v2_19_default.yaml \
  --out runs/v2_19_default
```

默认配置保留 TXT 中的 `sionna_tr38901_uma`、大规模阵列和 10 个 drop × 10000 个
observation window，适用于有足够内存及 Sionna 环境的正式实验。`fallback_to_numpy_if_unavailable`
只负责 Sionna 不可用时的后端回退，不保证大规模 Sionna 张量在低内存机器上可运行。
由于输出和信道计算规模较大，建议先用 smoke 配置验证代码：

```bash
python -m beam_sls.run \
  --config configs/v2_19_service_beam_statistics_smoke.yaml \
  --out runs/v2_19_service_beam_statistics_smoke
```

需要修改实验规模时，复制 `configs/v2_19_default.yaml` 后调整
`system.num_drops`、`service_beam_statistics.observations_per_drop`、
`candidate_ues_per_sector_per_drop` 和 traffic 参数即可。若要保存每个 UE 的全部窗口
arrival 记录，将 `save_per_ue_arrival_samples` 改为 `true`。

## 4. 输出文件与解读

### 4.1 静态候选 UE Beam Sharing

| 文件 | 粒度 | 含义 |
|---|---|---|
| `service_beam_candidate_ue_count_samples.csv` | `(drop, beam)` | 全部候选 UE 中 best beam 为该 beam 的 UE 数 `candidate_ue_count`。每个 drop、每个 beam 都有一行，包括 0。 |
| `service_beam_candidate_ue_count_pmf.csv` | `(beam, ue_count)` | (P(C_b=k))，分母为 `num_drops`。 |
| `service_beam_candidate_ue_count_summary.csv` | `beam` | 静态候选 UE sharing 数的均值、方差、P50/P90/P95。 |

每个 drop 必须满足：

```text
sum_b candidate_ue_count[b, drop] = num_candidate_ues_per_drop
```

这组文件用于研究固定候选 UE 是否集中上报同一个服务 beam，与 FTP arrival rate 无关。

### 4.2 窗口 Active UE Beam Collision

| 文件 | 粒度 | 含义 |
|---|---|---|
| `ftp_ue_arrival_samples.csv` | `(drop, observation, ue_id)` | `num_arrivals`、`has_arrival` 以及该 UE 的缓存 service beam。 |
| `service_beam_active_ue_count_samples.csv` | `(drop, observation, beam)` | 窗口内 active UE 数 `active_ue_count`。同时保留同值字段 `ue_with_arrival_count` 便于旧脚本迁移。每个窗口、每个 beam 都有一行，包括 0。 |
| `service_beam_active_ue_count_pmf.csv` | `(beam, ue_count)` | (P(N_b=k))，分母为 `num_drops × observations_per_drop`。 |
| `service_beam_active_ue_count_summary.csv` | `beam` | active UE collision 数的均值、方差、idle/busy 概率和 P50/P90/P95。 |

窗口守恒关系为：

```text
sum_b active_ue_count[b, drop, observation]
  = count(ftp_ue_arrival_samples[drop, observation].has_arrival == 1)
```

因此：

- 静态 candidate PMF 看“所有候选 UE 的 beam sharing”；
- active PMF 看“每个 observation window 内新激活 UE 的 beam collision”；
- `active_ue_count` 不表示队列中的 active/backlogged UE，也不表示文件仍在传输。

另外保留 `service_beam_ue_cache.csv`、`service_beam_selection_probability.csv`、
`beams.csv`、`drops.csv`、`runtime_phases.csv` 等诊断输出。

## 5. 测试与兼容性

新增/更新测试覆盖：

1. 静态 candidate UE beam count 在每个 drop 守恒；
2. 每个 arrival 记录的 `ue_id` 存在于本 drop 的缓存；
3. 同一 UE 在不同窗口复用相同 best beam；
4. 同一 UE 单窗口多次 arrival 只计一个 active UE；
5. 每窗口、每 beam 都有样本行，包括零计数；
6. active 和 candidate 两类 PMF 分别归一化；
7. 零 arrival rate 时 active PMF 满足 (P(N_b=0)=1)；
8. 主流程不调用匿名 multinomial beam 分配；
9. 默认 scheduling smoke 保持通过。

验证命令：

```bash
python -m compileall -q beam_sls tests
python -m pytest tests/test_service_beam_statistics.py -q --tb=short
python -m pytest tests/test_smoke.py -q --tb=short -x
python -m pytest -q --tb=short
```

本版本的统计边界是 arrival-only：不包含服务时间、传输完成、调度吞吐或跨窗口队列。

# 更新说明 v2.19：全局服务波束碰撞用户数分布

## 1. 修改原因

原代码对每个 beam 分别保存 `active_samples_by_beam[beam_index]`，随后生成逐 beam PMF。
这不符合本次最终统计目标：需要把所有 `(drop, observation, beam)` 的非零 beam 样本
放在同一个全局样本池中，直接统计一个服务 beam 样本中承载了几个 active UE。

本次最终统计量定义为：

```text
X[u,t] ~ Poisson(arrival_rate_per_ue_s × T_obs)
A[u,t] = 1{X[u,t] > 0}
N[b,d,t] = sum_u A[u,t] × 1{best_service_beam[u,d] == b}
```

只保留 `N[b,d,t] > 0` 的 beam-observation 样本：

```text
S = {(d,t,b) | N[b,d,t] > 0}
P(K=k) = count({s in S | N[s] = k}) / |S|
```

最终 PMF 的支持范围是 `1, 2, 3, ...`：

- `P(K=1)`：无碰撞概率，一个非空服务 beam 样本只有 1 个 active UE；
- `P(K>=2)`：服务波束碰撞概率，一个服务 beam 被至少 2 个 active UE 共同使用。

零计数 beam-observation 不进入最终全局 PMF，但仍保留在逐 beam 明细中，用于检查
空闲 beam 和窗口守恒。

## 2. 代码修改

### 2.1 全局碰撞 PMF

`beam_sls/service_beam_statistics.py` 新增 `_global_collision_pmf_rows()`：

1. 遍历所有 beam 的窗口 count；
2. 过滤 `count == 0`；
3. 合并所有非零 count；
4. 统计 `ue_count`、`sample_count` 和 `probability`；
5. 额外计算 `no_collision_probability` 和 `collision_probability`。

主输出文件：

```text
metrics/service_beam_global_collision_pmf.csv
```

字段为：

```text
ue_count
sample_count
probability
```

示例结构：

| ue_count | sample_count | probability |
|---:|---:|---:|
| 1 | 392451 | 0.9821 |
| 2 | 7022 | 0.01757 |
| 3 | 127 | 0.000318 |
| 4 | 5 | 0.0000125 |

`metrics/service_beam_global_collision_summary.csv` 保存：

```text
num_beam_observation_samples
num_nonzero_beam_observation_samples
num_no_collision_samples
num_collision_samples
no_collision_probability
collision_probability
```

逐 beam 文件仍然保留：

```text
service_beam_active_ue_count_samples.csv
service_beam_active_ue_count_pmf.csv
service_beam_active_ue_count_summary.csv
```

但这些文件现在是诊断明细，不再作为最终全局 collision PMF。

### 2.2 每小区平均到达 UE 数

新增：

```text
metrics/cell_arrival_ue_count_samples.csv
metrics/cell_arrival_ue_count_summary.csv
```

每个 `(drop, observation, cell)` 保存：

- `active_ue_count` / `arriving_ue_count`：窗口内至少一次 arrival 的不同 UE 数；
- `num_arrivals`：该小区所有 UE 的 Poisson arrival event 总次数；
- `candidate_ue_count`：该小区本 drop 的候选 UE 数。

每小区 summary 保存：

- `mean_arriving_ue_count`：每窗口平均有 arrival 的不同 UE 数；
- `mean_active_ue_count`：与上者相同的显式 active UE 字段；
- `mean_num_arrivals`：每窗口平均 arrival event 次数；
- active UE 数的方差和 P50/P90/P95。

因此一个 UE 在一个窗口内发生多次 arrival 时，`mean_arriving_ue_count` 只计 1，
`mean_num_arrivals` 会计入多次。

### 2.3 原有固定 UE 逻辑

每个 drop 仍按以下流程执行：

```text
固定生成候选 UE 和位置
  → 生成信道并关联 serving cell
  → 每个 UE 选择最大 service_power_w 的 best TX beam
  → 相同功率取最小全局 beam_index
  → 按 ue_id 缓存 best service beam
  → 每窗口对每个 UE 独立 Poisson arrival
  → 通过 ue_id 查询 beam 缓存并统计 active UE
```

窗口结束后不保留队列、文件、服务时间或传输状态。统计模式不调用 scheduler、link
adaptation、OLLA、ACK/NACK、TBLER、吞吐计算或 `run_tti_loop`。

## 3. 默认 YAML 与使用方法

根据 `v2.19 yaml.txt` 已整理默认配置：

```text
configs/v2_19_default.yaml
```

该配置包含 7 个站点、21 个小区、每小区 10 个候选 UE，即每个 drop 210 个候选 UE，
并设置：

```yaml
system:
  run_mode: service_beam_statistics

service_beam_statistics:
  observations_per_drop: 10000
  observation_interval_ms: 100.0
  candidate_ues_per_sector_per_drop: 10
  include_zero_count: true
  traffic:
    model: 3gpp_ftp_model_3_arrival_only
    arrival_rate_per_ue_s: 0.2
    file_size_mbytes: 0.5
```

正式运行：

```bash
python -m beam_sls.run \
  --config configs/v2_19_default.yaml \
  --out runs/v2_19_default
```

正式配置规模较大，默认使用 Sionna 信道和 10 个 drop × 10000 个窗口，需要足够内存。
建议先运行 smoke 配置：

```bash
python -m beam_sls.run \
  --config configs/v2_19_service_beam_statistics_smoke.yaml \
  --out runs/v2_19_service_beam_statistics_smoke
```

实验规模可调整：

- `system.num_drops`：radio drop 数；
- `service_beam_statistics.observations_per_drop`：每 drop 窗口数；
- `service_beam_statistics.observation_interval_ms`：窗口长度 `T_obs`；
- `candidate_ues_per_sector_per_drop`：每小区候选 UE 数；
- `traffic.arrival_rate_per_ue_s`：每 UE arrival rate；
- `save_per_ue_arrival_samples: true`：保存所有 UE 的窗口到达记录，便于验收。

## 4. 输出文件

| 文件 | 粒度 | 用途 |
|---|---|---|
| `service_beam_ue_cache.csv` | `(drop, ue_id)` | UE 位置、serving cell、best TX/RX beam 和 service power。 |
| `ftp_ue_arrival_samples.csv` | `(drop, observation, ue_id)` | `num_arrivals`、`has_arrival` 和缓存 beam。默认只保存有 arrival 的 UE。 |
| `service_beam_global_collision_pmf.csv` | `(ue_count)` | 最终全局非空 beam-observation collision PMF。 |
| `service_beam_global_collision_summary.csv` | 全局一行 | 无碰撞概率和碰撞概率。 |
| `service_beam_active_ue_count_samples.csv` | `(drop, observation, beam)` | 逐 beam active UE 明细，包含零计数。 |
| `service_beam_candidate_ue_count_samples.csv` | `(drop, beam)` | 全部候选 UE 的静态 beam sharing 数。 |
| `cell_arrival_ue_count_samples.csv` | `(drop, observation, cell)` | 每小区 active UE 数和 raw arrival event 数。 |
| `cell_arrival_ue_count_summary.csv` | `cell` | 每小区平均到达 UE 数和平均 arrival 次数。 |

窗口守恒关系：

```text
sum_b active_ue_count[b,d,t]
  = sum_u 1{has_arrival[u,d,t] == 1}
```

## 5. 验证

```bash
python -m compileall -q beam_sls tests
python -m pytest tests/test_service_beam_statistics.py -q --tb=short
python -m pytest tests/test_smoke.py -q --tb=short -x
python -m pytest -q --tb=short
```

新增测试覆盖全局 PMF 非零样本池、`P(K=1)`、`P(K>=2)`、每小区窗口守恒和小区平均
到达 UE 数；默认 `scheduling` 模式保持兼容。

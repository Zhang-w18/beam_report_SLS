# 更新说明 v2.19：固定 UE 的服务波束 UE 数概率分布统计

## 1. 更新目的与原因

新增独立运行模式 `service_beam_statistics`，用于在固定服务窗口内统计每个服务波束
对应的 `ue_with_arrival_count` 概率分布。

旧方案先生成匿名 cell 级到达总数，再按经验波束选择概率做 multinomial 分配，无法
证明业务到达属于哪个已经撒点的 UE，也无法验证同一 UE 跨窗口复用同一个服务波束。
本次实现改为固定 UE 集合上的 arrival-only 过程，业务记录始终关联具体 `ue_id`。

## 2. 更新方式

每个 radio drop 的数据流为：

```text
make_topology() 固定 UE 及位置
  → 生成信道并按宽带平均 RSRP 关联 serving_cell
  → 对 serving cell 的可用 TX beam 计算 service_power_w
  → 选择功率最大的 beam（相同功率取最小全局 beam_index）并按 ue_id 缓存
  → 每个窗口对每个固定 UE 独立抽样 Poisson 到达次数
  → 通过 ue_id 查询缓存 beam
  → 每个 beam 统计有到达的不同 UE 数并累计 PMF
```

核心模型是：

```text
X[u,t] ~ Poisson(arrival_rate_per_ue_s × observation_interval_s)
A[u,t] = 1{X[u,t] > 0}
N[b,t] = sum_u A[u,t] * 1{best_service_beam[u] == b}
```

同一 UE 在一个窗口内即使 `num_arrivals > 1`，`N[b,t]` 仍只增加 1。窗口结束时不
保留队列，不模拟文件完成时间、调度吞吐或业务积压，因此输出使用
`ue_with_arrival_count`，不使用 `active_ue_count` 或 `backlogged_ue_count`。

`beam_sls/ftp_traffic.py` 新增 `FTPArrivalOnlyConfig`、`sample_ue_arrivals()` 和
`count_arriving_ues_by_beam()`；旧的 `sample_cell_arrivals()` 和
`allocate_arrivals_to_beams()` 不再属于统计主流程。

统计模式不初始化或调用 feedback、evaluation matrix、scheduler、link adaptation、
OLLA、ACK/NACK、TBLER、吞吐计算和 `run_tti_loop`。CLI 对统计模式采用独立分流，
调度模式仍保持 `system.run_mode: scheduling` 默认值。

拓扑/信道流和业务流按 `(random_seed, drop, stream)` 分离：stream 0 用于拓扑和信道，
stream 1 用于业务到达。进入新 drop 后重新生成 UE、信道和 beam 缓存；不同 drop 中
数值相同的 `ue_id` 不代表同一个物理 UE。

## 3. 更新后如何使用

### 3.1 YAML 配置

```yaml
system:
  run_mode: service_beam_statistics
  num_drops: 10
  random_seed: 20260610

service_beam_statistics:
  observations_per_drop: 10000
  observation_interval_ms: 100.0
  candidate_ues_per_sector_per_drop: 100
  include_zero_count: true
  save_per_ue_arrival_samples: false
  traffic:
    model: 3gpp_ftp_model_3_arrival_only
    arrival_rate_per_ue_s: 0.2
    file_size_mbytes: 0.5
```

`candidate_ues_per_sector_per_drop` 会覆盖该模式使用的
`ue_drop.num_ut_per_sector`；设置为 `null` 时直接使用 `ue_drop` 配置。
`save_per_ue_arrival_samples: true` 适合验收测试，会保存每个 UE 的所有窗口记录；
设为 `false` 时只保存 `num_arrivals > 0` 的 UE，以控制输出规模。

可直接运行：

```bash
python -m beam_sls.run \
  --config configs/v2_19_service_beam_statistics_smoke.yaml \
  --out runs/v2_19_service_beam_statistics
```

正式运行时按机器资源增大 `num_drops`、`observations_per_drop` 和候选 UE 数。

### 3.2 输出文件

输出位于 `metrics/`：

| 文件 | 粒度与用途 |
|---|---|
| `service_beam_ue_cache.csv` | 每 `(drop, ue_id)` 一行，含 `x_m`、`y_m`、`serving_cell`、`best_service_beam_index`、`best_service_beam_id`、`best_rx_beam_index`、`service_power_w`。 |
| `ftp_ue_arrival_samples.csv` | 每个保存的 `(drop, observation, ue_id)` 到达次数，含 `num_arrivals`、`has_arrival` 和缓存 beam 信息；可只输出有到达 UE。 |
| `service_beam_ue_count_samples.csv` | 每 `(drop, observation, beam)` 一行，含 `ue_with_arrival_count`；每个窗口每个 beam 都有行，包括 0。 |
| `service_beam_ue_count_pmf.csv` | 每 `(beam, ue_count)` 的 `sample_count`、`probability` 和 `conditional_probability_given_nonzero`。 |
| `service_beam_ue_count_summary.csv` | 每 beam 的观测数、均值、方差、idle/busy 概率及 P50/P90/P95。 |
| `service_beam_selection_probability.csv` | 每 `(drop, beam)` 的候选 UE 服务 beam 选择频率，便于解释空间/码本差异。 |

逐 beam PMF 使用：

```text
P_b(k) = sample_count(drop,t: N[b,t] = k)
         / (num_drops × observations_per_drop)
```

因此每个 beam 的 `probability` 之和为 1，并且包含 `P(N_b=0)`。窗口守恒关系为：

```text
sum_b ue_with_arrival_count[b,t]
  = count(ftp_ue_arrival_samples[t].has_arrival == 1)
```

## 4. 验证与兼容性

新增测试覆盖：具体 `ue_id` 关联、同一 UE 单窗口只计一次、最小全局 beam tie-break、
逐窗口守恒、零到达率、PMF 归一化、每窗口每 beam 的零计数行，以及统计主流程不调用
匿名多项式分配。默认调度配置仍为 `scheduling`；统计模式不会进入调度链路。

验证命令：

```bash
python -m compileall -q beam_sls tests
python -m pytest tests/test_service_beam_statistics.py -q --tb=short
python -m pytest tests/test_smoke.py -q --tb=short -x
```

当前限制是 arrival-only 口径不表示真实传输中的 active/backlogged UE。若要统计队列
积压，必须另行增加服务速率、文件完成和队列状态模型，不能直接解释本模式的 PMF。

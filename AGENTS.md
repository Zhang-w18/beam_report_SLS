# AGENTS.md

本文件适用于本仓库根目录及其全部子目录。目标是在保证修改正确、可验证、
不破坏用户现有工作的前提下，减少不必要的源码读取、日志输出和 token 消耗。

## 1. 开始任务前

处理代码分析、诊断或修改请求时，按以下顺序获取上下文：

1. 先阅读 [`docs/repository_memory.md`](docs/repository_memory.md)。
2. 查看 `git status --short`，确认工作区已有修改；这些修改默认属于用户。
3. 根据仓库记忆定位可能相关的模块、函数和测试。
4. 只有仓库记忆不足、已经过时，或与实际代码冲突时，才使用 `rg` 做定向搜索。
5. 实际代码、配置和测试始终是最终事实来源；仓库记忆不能替代代码验证。

不要在任务开始时盲目扫描整个仓库、读取完整大文件或输出完整 diff。

## 2. Token 控制与源码读取

### 2.1 搜索

- 先用 `rg --files` 定位文件，再用 `rg -n` 定位符号、定义和调用。
- 优先搜索准确的类名、函数名、配置键或输出字段。
- 避免类似 `rg -n "tti|drop|channel|schedule|..." .` 的全仓库宽泛搜索。
- 搜索结果可能较多时，限制目录、文件类型或具体文件。
- 已经从本轮上下文或仓库记忆得到的信息，不要无理由重复读取。

推荐：

```powershell
rg --files beam_sls tests docs
rg -n "def run_tti_loop|run_tti_loop\(" beam_sls tests
rg -n -C 20 "def run_tti_loop" beam_sls/link.py
```

### 2.2 读取源码

- 默认只读取目标函数、类及必要上下文，不读取整个大文件。
- 单次源码读取通常不超过 150 行；复杂函数可分段读取。
- 已知行号时使用 `Get-Content ... | Select-Object -Skip N -First M`。
- 需要理解调用链时，先读取入口和直接依赖，不进行无目的的深层追踪。
- 只有文件较短、结构未知且确有必要时，才读取完整文件。

### 2.3 Diff

检查修改时依次使用：

```powershell
git status --short
git diff --stat
git diff --name-only
git diff --check
git diff --unified=3 -- path/to/relevant_file.py
```

- 不默认输出整个工作区的 `git diff`。
- 只检查本任务相关文件；已有的无关修改不要展开、格式化或覆盖。
- 对大 diff，先看统计和文件列表，再逐文件、逐函数检查。
- 不把完整 diff 复制到最终答复，只总结关键变更。

### 2.4 日志和测试输出

- 先运行最小相关测试，再考虑扩大范围。
- pytest 优先使用 `-q --tb=short -x`；需要完整套件时去掉 `-x`。
- 长仿真先使用缩小的 drop、UE、beam、频点和 TTI 配置做 smoke test。
- 完整日志写入文件时，只读取错误、失败附近上下文或末尾摘要。
- 不向上下文加载数千行成功日志、进度日志或重复 warning。
- 测试依赖缺失时，先做可用的编译检查、定向单元调用或最小端到端验证，
  并准确说明未运行的部分。

## 3. 修改代码

- 使用 `apply_patch` 做有边界的人工修改。
- 保留用户现有的未提交修改，不执行 `git reset --hard`、`git checkout --`
  或其他覆盖性操作。
- 不因本任务顺手重构无关代码。
- 修改配置语义、输出格式、张量维度或核心流程时，同步更新相关测试和文档。
- 新增配置项时：
  - 在 `beam_sls/config.py` 提供安全的默认值；
  - 在 `docs/yaml_parameter_reference.md` 说明类型、范围和行为；
  - 需要用户直接启用时，在 README 或更新文档给出 YAML 示例。
- 新增输出时，说明生成条件、字段含义、统计粒度和解读方法。
- 涉及随机仿真时，注意随机种子、不同 case 的共同随机数以及 drop 间状态隔离。

## 4. 验证要求

验证强度与改动风险匹配：

1. 语法和格式：`python -m compileall`、`git diff --check`。
2. 局部逻辑：运行目标函数或相关单元测试。
3. 流程改动：运行缩小规模端到端仿真。
4. 输出改动：检查 CSV/JSON 是否生成、字段是否存在、统计粒度是否正确。
5. 默认行为改动：同时验证新模式和旧模式，或明确说明未覆盖的模式。

最终答复只需说明：

- 修改结果；
- 关键文件；
- 验证结果；
- 尚存限制或风险。

## 5. 仓库记忆维护

[`docs/repository_memory.md`](docs/repository_memory.md) 是本仓库的长期代码地图和
设计摘要。

- 架构、主流程、配置结构、张量约定、状态生命周期或主要输出发生实质变化时，
  同步更新仓库记忆。
- 小型 bug 修复、注释调整或不改变设计的局部重构，不必扩充仓库记忆。
- 仓库记忆保持简洁，记录稳定事实，不写临时调试日志或一次性实验结果。
- 若记忆与代码不一致，先以代码为准完成任务，再修正记忆。

## 6. “写入更新文档”个人规则

当用户明确要求“将本轮修改写在 `xxx` 更新文档里”或表达同等含义时：

1. 使用用户指定的文档名；文档不存在则新建，存在则在保留原内容的基础上追加
   本轮章节，不覆盖历史记录。
2. 本轮章节必须说明：
   - **更新目的与原因**：要解决什么问题，旧行为有什么不足；
   - **更新方式**：修改了哪些模块、核心逻辑和关键公式/数据流；
   - **更新后如何使用**：
     - YAML 如何配置；
     - 可直接参考的 bash 运行命令；
     - 新增或变化的输出文件、字段、统计粒度及解读方法；
   - **验证与兼容性**：运行了哪些测试，默认行为是否改变，已知限制是什么。
3. YAML 和 bash 示例必须与当前代码支持的参数一致，不能只写概念性伪代码。
4. 如果本轮没有新增输出，应明确写“无新增输出”，并说明现有哪个输出可用于验证。
5. 文档标题和章节风格尽量延续该更新文档已有格式。

除非用户明确提出此要求，不要为每次小修改自动创建新的版本更新文档。

## 7. 常用精简命令

```powershell
# 文件与符号定位
rg --files beam_sls tests configs docs
rg -n "目标函数名|配置键" beam_sls tests

# 有界读取
Get-Content beam_sls/sim.py | Select-Object -Skip 500 -First 120

# 有界 diff
git diff --stat
git diff --unified=3 -- beam_sls/sim.py
git diff --check

# 精简测试
python -m pytest tests/test_target.py -q --tb=short -x
python -m compileall -q beam_sls tests
```

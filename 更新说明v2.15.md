# 更新说明 v2.15：Hard-Conflict 排序、动态 Beam 唯一占用与干扰 MCS 明细

## 1. 更新目的与原因

本轮更新主要解决以下问题：

1. `hard_conflict_greedy` 在多个候选具有相同调度效用时，旧规则会先选择最高
   SNR，即使候选处于同一 MCS、SNR 差距很小，也可能删除更多后续候选。
2. 动态 shared-codebook 模式原来只限制每个 TRP 的总并发数，没有禁止同一 TRP
   的同一个 beam index 同时分配给两个 UE。
3. 七站点对比 YAML 虽然使用 `proportional_fair` 目标，但需要明确启用连续 TTI，
   才能在每个 TTI 使用最新的 PF 历史吞吐重新调度。
4. 现有 Top-K 干扰明细已经记录 SINR 降幅，但缺少对应的 MCS 降幅和 outage，
   不方便判断某个干扰 beam 是否造成实际链路等级下降。
5. 需要明确区分 drop 内固定的 Gamma 测量与逐 TTI 变化的实际小尺度信道。

本轮基于以下配置文件直接更新：

```text
configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml
```

## 2. Hard-Conflict 候选排序规则

### 2.1 新排序顺序

`hard_conflict_greedy` 每轮对当前仍可选的 `(UE, beam)` 候选执行：

1. 计算候选加入当前已选集合后的边际效用：

   ```text
   marginal_utility(candidate)
     = objective(selected + candidate) - objective(selected)
   ```

2. 优先选择当前边际效用最大的候选。
3. 边际效用相同时，如果候选 MCS 相同，并且候选 SNR 跨度不超过配置门限，
   优先选择对当前候选池冲突影响更小的候选。
4. 如果 SNR 差距超过门限，优先选择 SNR 更高的候选。
5. SNR 或冲突影响仍相同时，按 `(UE ID, beam index)` 确定性排序。

冲突影响定义为：

```text
conflict_impact(candidate)
  = 当前候选池中与 candidate 存在任一方向冲突的其他 UE 候选节点数
```

该规则的目的不是降低主调度效用，而是在主效用相同、无线质量近似等价时，
尽量保留更多后续可调度候选。

### 2.2 SNR 近似等价门限

新增调度参数：

```yaml
scheduler:
  hard_conflict_snr_close_db: 1.0
```

含义：

- 必须为非负 dB；
- 默认值为 `1.0`；
- 同 MCS 候选的 SNR 跨度不超过该值时，优先低冲突影响；
- 跨度更大时，优先高 SNR。

逐轮调试信息继续写入：

```text
metrics/scheduler_iterations.csv
```

相关 metadata/字段包括：

```text
selected_marginal_utility
selected_conflict_impact
selected_tiebreak_mcs
selected_tiebreak_snr_db
tiebreak_rule
```

主要代码：

- `beam_sls/scheduler.py::hard_conflict_greedy_schedule`
- `beam_sls/config.py::DEFAULT_CONFIG`

## 3. 动态 TRP 的 Beam 唯一占用约束

### 3.1 旧行为

动态 beam 分配模式原来只检查：

```text
同一 TRP 已调度 beam 数
  <= _resolved.max_parallel_beams_per_trp
```

因此当容量为 2 时，两个 UE 可能同时选择同一个 TRP 的同一个本地 beam index。

### 3.2 新行为

启用：

```yaml
scheduler:
  use_panel_constraint: true
```

后，动态模式必须同时满足：

```text
1. 每个 TRP 的总并发 beam 数不超过 RF capacity；
2. 同一个 (cell, TRP, local beam index) 每个 TTI 最多分配给一个 UE。
```

例如：

```text
UE 0 -> TRP 0, beam 5
UE 1 -> TRP 0, beam 5    非法

UE 0 -> TRP 0, beam 5
UE 1 -> TRP 0, beam 8    容量允许时合法
```

该约束统一应用于：

- `greedy`
- `adaptive_lambda_greedy`
- `hard_conflict_greedy`
- `exhaustive`
- optimized 和 legacy/reference greedy 路径

固定 panel 模式继续保持每个 panel 一个 beam 的原有约束。

主要代码：

- `beam_sls/scheduler.py::_panel_constraint_ok`
- `beam_sls/scheduler.py::_beam_occupancy_key`
- `beam_sls/scheduler.py::_resource_available`

## 4. 逐 TTI 比例公平调度配置

### 4.1 代码支持方式

逐 TTI PF 需要同时配置：

```yaml
system:
  continuous_tti:
    enabled: true

scheduler:
  objective: proportional_fair
```

连续模式中，每个 drop 只执行一次：

```text
UE 撒点
完整大尺度/基础信道生成
Gamma 测量
feedback report 生成
```

每个 warmup 和正式 TTI 执行：

```text
推进小尺度信道
  -> 使用当前 Tbar 重新调度
  -> 实际链路 SINR/MCS/TBLER/ACK-NACK
  -> 更新 OLLA
  -> 使用实际 goodput 更新所有 UE 的 PF Tbar
```

未调度 UE 的本 TTI goodput 按 0 更新。PF 状态按 evaluation case 隔离，并在新
drop 开始时重置。

### 4.2 七站点 YAML

目标 YAML 已配置为：

```yaml
system:
  num_drops: 50
  num_tti_per_drop: 200
  continuous_tti:
    enabled: true
    duration_ms: 25.0
    num_tti: 200
    warmup_tti: 50

pdsch:
  slot_duration_ms: 0.125

ue_drop:
  speed_kmh: 3.0

scheduler:
  objective: proportional_fair
  hard_conflict_snr_close_db: 1.0
  pf_tbar_init_mbps: 1.0
  pf_averaging_window_tti: 100
```

其中：

```text
50 drops
50 warmup TTI/drop
200 measured TTI/drop
PF EWMA alpha = 1/100
```

`continuous_tti.num_tti` 非空时优先于 `duration_ms`；此处两者保持一致：

```text
200 * 0.125 ms = 25 ms
```

## 5. Drop 内逐 TTI 信道变化

不同 TTI 不会重新调用完整信道生成器，也不是每个 TTI 独立实例化一份
TR 38.901 信道。

每个 drop 先生成基础信道：

```text
H0[UE, TX_UNIT, FREQ, RX_AE, TX_AE]
```

路径损耗、阴影衰落、时延/频率结构和空间特征在 drop 内固定。随后为每个
`(UE, TX unit)` 维护一个复数小尺度状态：

```text
g[t+1] = rho*g[t] + sqrt(1-rho^2)*z[t]
```

其中：

```text
rho = J0(2*pi*f_D*T_TTI)
f_D = v*f_c/c
```

`rho` 不是 YAML 中的直接参数，而是由以下配置计算：

```yaml
ue_drop:
  speed_kmh: 3.0

scenario:
  carrier_frequency_ghz: 30.0

pdsch:
  slot_duration_ms: 0.125
```

实际信道近似为：

```text
H[t, UE, TX unit] = H0[UE, TX unit] * g[t, UE, TX unit]
```

注意：

- `z[t]` 是逐 TTI 新生成的独立复高斯创新；
- `g[t]` 通过 `rho` 与前一 TTI 相关，因此不同 TTI 不是独立信道采样；
- `speed_kmh=0` 时 `rho=1`，信道保持不变；
- 当前是 link-wise Jakes 近似，同一 `(UE, TX unit)` 的频点和天线维共享一个
  复数状态，不会逐路径、逐频点或逐天线独立演化；
- Gamma 固定是因为每个 drop 只测量一次，不表示实际 TTI SINR 一定不变；
- 实际链路评估使用变化后的 `H[t]`，不同 TX unit 的独立状态可能改变相对干扰
  强度。

主要代码：

- `beam_sls/channel.py::DopplerChannelEvolver`
- `beam_sls/sim.py::run_simulation`

## 6. Top-K 干扰 SINR/MCS 明细

### 6.1 统计粒度

现有文件：

```text
metrics/topk_interference_details.csv
```

保持每个：

```text
(drop, UE, service candidate beam, Top-K interferer beam)
```

一行。该文件对应每个 drop 唯一一次的 Gamma/feedback 测量，不增加 TTI 维度，
也不会在连续 TTI 模式下重复写 200 份相同测量。

当前范围与 `topk_conflict_id` 上报一致：每个服务候选只记录实际上报的
`feedback.conflict_top_k2` 个强干扰 beam，不记录调度簇内全部未上报 beam。

### 6.2 SINR 指标

对一个服务 beam 和一个干扰 beam：

```text
service_snr_db = 10*log10(S/N)
pair_sinr_db   = 10*log10(S/(I+N))
sinr_loss_db   = service_snr_db - pair_sinr_db
```

### 6.3 MCS 指标

使用与调度相同的 link-adaptation/lookup 后端分别映射：

```text
service_mcs = MCS(service SNR)
pair_mcs    = MCS(pair SINR)
mcs_loss    = service_mcs - pair_mcs
```

同时记录：

```text
service_outage
pair_outage
```

outage 字段用于区分：

- `pair_mcs=0` 但最低 MCS 仍满足目标 BLER；
- `pair_mcs=0` 且已经低于目标 BLER。

### 6.4 CSV 字段

`metrics/topk_interference_details.csv` 现在包含：

```text
drop
scheme
ue_id
site_id
serving_cell
scheduling_cluster
service_candidate_rank
service_beam_index
service_beam_id
interferer_rank
interferer_beam_index
interferer_beam_id
service_snr_db
pair_sinr_db
sinr_loss_db
service_mcs
pair_mcs
mcs_loss
service_outage
pair_outage
```

`metrics/reports.csv` 的 `report_json.candidates[].interference_details` 同步包含
上述 SINR、MCS 和 outage 明细。

本轮无新增输出文件，只扩展已有：

```text
metrics/topk_interference_details.csv
metrics/reports.csv
metrics/scheduler_iterations.csv
```

主要代码：

- `beam_sls/feedback.py::make_reports`
- `beam_sls/sim.py::run_simulation`

## 7. 运行方式

完整配置运行：

```bash
python -m beam_sls.run \
  --config configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml \
  --out runs/v2_15_7site21cell_persite_pf_topk
```

建议先执行缩小规模 smoke：

```bash
python -m beam_sls.run \
  --config configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml \
  --out runs/v2_15_smoke \
  --num-drops 1 \
  --num-tti 2 \
  --warmup-tti 1 \
  --skip-heatmap
```

检查干扰明细：

```bash
python - <<'PY'
import csv

path = "runs/v2_15_smoke/metrics/topk_interference_details.csv"
with open(path, newline="", encoding="utf-8") as handle:
    rows = list(csv.DictReader(handle))

for row in rows[:10]:
    print(
        row["drop"],
        row["ue_id"],
        row["service_beam_id"],
        row["interferer_beam_id"],
        row["sinr_loss_db"],
        row["mcs_loss"],
        row["pair_outage"],
    )
PY
```

## 8. 验证与兼容性

新增或扩展测试：

- `tests/test_scheduler_v210.py`
  - 动态 TRP 同 beam 重复分配会被拒绝；
  - greedy、adaptive-lambda、hard-conflict 和 exhaustive 均遵守唯一占用；
  - 同 MCS、SNR 差距不超过 1 dB 时优先低冲突影响；
  - SNR 差距超过 1 dB 时优先高 SNR；
  - 剩余平局按稳定 UE/beam ID 排序。
- `tests/test_array_config.py`
  - 七站点 YAML 启用 50 drops、200 measured TTI、50 warmup TTI 和 PF 窗口 100。
- `tests/test_smoke.py`
  - Top-K pair SINR 使用相同链路抽象映射 MCS；
  - 验证 `service_mcs`、`pair_mcs`、`mcs_loss` 和 outage；
  - 验证 CSV 字段存在；
  - 验证服务 beam 和 pair SINR 的 link-adaptation 调用范围。

本轮实际完成：

```text
python -m compileall -q beam_sls tests
git diff --check
```

结果通过。

由于当前系统 Python 缺少 `pytest`、`PyYAML` 和 NumPy，未通过标准
`python -m pytest` 运行完整套件。使用 Codex bundled Python 和最小 YAML stub
直接执行了：

```text
13 个 scheduler 无 fixture 测试
4 个 PF/连续 TTI 无 fixture 测试
25 个 smoke 无 fixture 测试
```

上述直接测试均通过。完整端到端 smoke 在当前 sandbox 中因 bundled Python
创建输出目录时的 Windows ACL 权限限制未完成，应在服务器依赖环境中执行：

```bash
python -m pytest \
  tests/test_scheduler_v210.py \
  tests/test_pf_continuous_tti.py \
  tests/test_array_config.py \
  tests/test_smoke.py \
  -q --tb=short -x
```

兼容性说明：

- `hard_conflict_snr_close_db` 有安全默认值 `1.0`，旧 YAML 不配置也可运行；
- 动态模式新增的同 TRP/同 beam 唯一占用是硬约束，可能改变旧版本 schedule；
- 固定 panel 模式约束保持不变；
- `topk_interference_details.csv` 保留全部旧字段，只追加 MCS、outage 和簇上下文字段；
- `ServiceCandidate.interference_details` 仍是可选字段，旧的手工构造方式保持兼容；
- Pair MCS 计算会增加 Top-K report 生成阶段的 link-adaptation 查询量；
- Gamma 和 feedback 仍为每 drop 一次；PF 调度和实际链路评估为每 TTI 一次；
- Top-K 干扰明细是测量值，不应当作逐 TTI 实际 post-scheduling SINR。

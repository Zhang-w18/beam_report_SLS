# 更新说明 v2.13：同指标冲突影响排序、Top-K 干扰明细与 PF 对比配置

## 1. 更新目的与原因

本轮更新基于：

```text
configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml
```

主要解决三个问题：

1. SU-SNR 经 MCS 量化后，不同 `(UE, beam)` 候选经常得到完全相同的 rate 或
   PF 调度指标。旧实现此时主要依赖候选顺序、UE ID 或 beam index 打破平局，
   没有利用 Top-K 已上报的冲突信息，可能选中会删除或惩罚更多后续候选的节点。
2. 旧的 Top-K report 只保存冲突 beam ID，不能直接观察每个干扰 beam 对服务
   波束造成的 SINR 损失。
3. 需要在同一七站点、21 cell、per-site 场景中，以比例公平目标对比 baseline
   greedy、Top-K 自适应 lambda greedy 和 Top-K 硬冲突 greedy。

## 2. 调度排序更新

### 2.1 同指标时优先选择冲突影响最小的候选

有限反馈 greedy 和 hard-conflict greedy 的主排序指标保持不变：

```text
sum-rate:              SU rate
proportional-fair:     SU rate / Tbar
adaptive lambda:       PF-weighted SU rate - lambda * existing conflict count
```

当本轮多个候选的调度指标完全相同时，新增次级排序规则：

```text
conflict_impact(candidate)
  = 与该候选存在任一方向冲突的、当前仍可选的其他 UE 候选节点数量
```

优先选择 `conflict_impact` 最小的候选。冲突影响仍相同时：

- optimized/legacy limited-feedback greedy 保留原候选顺序；
- hard-conflict greedy 再按 `(UE ID, beam index)` 确定性打破平局。

baseline 没有 conflict ID，因此所有 baseline 候选的冲突影响均为零，原有
确定性顺序不变。full-Gamma greedy 的排序逻辑不受本轮修改影响。

逐轮统计新增：

```text
equal_metric_candidate_count
selected_conflict_impact
```

它们写入：

```text
metrics/scheduler_iterations.csv
```

主要代码：

- `beam_sls/scheduler.py::_limited_feedback_adjacency`
- `beam_sls/scheduler.py::_candidate_conflict_impact`
- `beam_sls/scheduler.py::_choose_best_limited_candidate`
- `beam_sls/scheduler.py::hard_conflict_greedy_schedule`

## 3. Top-K 干扰明细记录

### 3.1 指标定义

对 `topk_conflict_id` 中每个服务候选 beam 及其每个 Top-K 强干扰 beam，记录：

```text
service_snr_db = 10*log10(S/N)
pair_sinr_db   = 10*log10(S/(I+N))
sinr_loss_db   = service_snr_db - pair_sinr_db
```

`sinr_loss_db` 越大，表示该干扰 beam 对当前服务 beam 造成的下降越严重。
干扰 beam 的排名按 `pair_sinr_db` 从低到高，即最强干扰排在前面；完全相同时
按 beam index 确定性排序。

### 3.2 report_json

`metrics/reports.csv` 中每个服务候选新增：

```text
interference_details
```

其中每个条目包含：

```text
interferer_beam_index
interferer_beam_id
service_snr_db
pair_sinr_db
sinr_loss_db
```

### 3.3 新增逐行 CSV

新增：

```text
metrics/topk_interference_details.csv
```

统计粒度为每个：

```text
(drop, scheme, UE, service beam, interferer beam)
```

字段包括：

```text
drop
scheme
ue_id
service_candidate_rank
service_beam_index
service_beam_id
interferer_rank
interferer_beam_index
interferer_beam_id
service_snr_db
pair_sinr_db
sinr_loss_db
```

主要代码：

- `beam_sls/feedback.py::ServiceCandidate`
- `beam_sls/feedback.py::make_reports`
- `beam_sls/sim.py::run_simulation`

## 4. v2.13 对比 YAML

本轮直接更新以下配置文件，不新增另一份场景 YAML：

```text
configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml
```

关键配置如下：

```yaml
rf_architecture:
  txru_connectivity: panel_polarization_subarray
  allow_independent_polarization_beams: false
  num_txru: 2
  max_parallel_beams_per_trp: auto

feedback:
  schemes:
    - baseline
    - topk_conflict_id
  service_beam_top_k1: 2
  oracle_service_beam_top_k: 2
  conflict_top_k2: 2

evaluation:
  matrix:
    baseline: [greedy]
    topk_conflict_id: [adaptive_lambda_greedy, hard_conflict_greedy]
  references:
    baseline: baseline__greedy

scheduler:
  cluster_mode: per_site
  objective: proportional_fair
  max_mu_order: auto
  algorithm: adaptive_lambda_greedy
  use_panel_constraint: true
  conflict_penalty_mode: adaptive
  adaptive_lambda_alpha: 0.20
```

实际产生三个对比 case：

```text
baseline__greedy
topk_conflict_id__adaptive_lambda_greedy
topk_conflict_id__hard_conflict_greedy
```

### 4.1 动态 beam 分配与并发容量

`allow_independent_polarization_beams: false` 使当前 TRP 使用共享单 panel 码本，
调度后再动态分配所选 codeword，不再将候选预绑定到两个独立极化 TX unit。

该 YAML 的阵列只有一个物理 panel，因此解析结果为：

```text
dynamic_beam_assignment = true
max_parallel_beams_per_trp = 1
max_mu_order = 3 per site
network-wide maximum = 21
```

如果需要“每 TRP 两个动态并发 beam”，必须进一步配置两个物理 panel 或采用
具有两个独立全阵列发射资源的 RF 架构；仅设置 `num_txru: 2` 不会把单物理 panel
的动态并发容量提高到 2。

## 5. 运行方式

服务器运行命令：

```bash
python -m beam_sls.run \
  --config configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml \
  --out runs/v2_13_7site21cell_persite_pf_baseline_topk
```

YAML 保持：

```text
50 drops
50 warmup TTI/drop
200 measured TTI/drop
per_site static scheduling clusters
coverage heatmap disabled
```

## 6. 验证与兼容性

新增或更新的测试：

- `tests/test_scheduler_v210.py`
  - 验证 adaptive-lambda optimized/reference 路径和 hard-conflict greedy
    在同 rate 时都选择冲突影响最小的候选。
- `tests/test_smoke.py`
  - 验证 Top-K `S/N`、`S/(I+N)`、下降 dB、JSON 序列化以及新增 CSV。
- `tests/test_array_config.py`
  - 验证指定 YAML 的 PF、三 case 矩阵、Top-2/Top-2、动态 beam 分配和
    解析后的 MU order。

已完成：

```text
python -m compileall -q beam_sls tests
git diff --check
```

结果均通过。

当前 Windows 系统 Python 缺少 `numpy`、`PyYAML` 和 `pytest`，因此本轮未在本机
执行 pytest 或完整仿真。应在服务器项目环境中运行：

```bash
python -m pytest \
  tests/test_scheduler_v210.py \
  tests/test_array_config.py \
  tests/test_smoke.py \
  -q --tb=short
```

兼容性说明：

- `ServiceCandidate.interference_details` 是新增的可选尾部字段，手工构造旧候选的
  调用方式保持兼容。
- baseline、full-Gamma 和 exhaustive 的主目标定义没有改变。
- `reports.csv` 的原有字段保留，仅在 `report_json` 中增加干扰明细。
- 新增 `topk_interference_details.csv` 和 scheduler iteration 字段，不删除旧输出。
- 指定 YAML 的默认目标、evaluation case、候选数和 RF 并发语义已经改变，旧结果
  与 v2.13 结果不应视为只有调度算法一项差异。

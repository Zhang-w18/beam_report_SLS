# 更新说明 v2.16：统一 PDSCH MCS/TBS 语义与修正 OLLA 因果关系

## 1. 更新目的与原因

本轮更新解决 MCS index 在不同链路阶段具有不同含义的问题，并修正 OLLA
使用调度器不可观测信息选择 MCS 的因果错误。

旧代码存在两套 MCS 语义：

- Sionna SYS ILLA 按 TS 38.214 Table 5.1.3.1-1（PDSCH Table 1）解释
  MCS index；
- `beam_sls/mcs.py` 使用一套错位的本地频谱效率数组计算 predicted rate
  和简化 TBS。

例如，Sionna 将 MCS 10 解释为：

```text
Qm = 4
R  = 340/1024
η  = 1.328125
```

旧 `mcs.py` 却将 MCS 10 解释为：

```text
η = 2.7305
```

因此，ILLA 选择出的 MCS、调度器记录的 `predicted_rate_mbps` 和 ACK 后计入
`goodput_bits` 的 TBS 并不属于同一张 MCS 表。

此外，旧 OLLA 路径使用：

```text
mcs_selection_sinr_db = effective_sinr_db - olla_offset_db
```

其中 `effective_sinr_db` 是合并实际 schedule 后，根据真实信道和实际共调度干扰
计算出的仿真真值。实际调度器在传输前无法知道该值，因此不能据此选择本次传输
MCS。

## 2. 更新方式

### 2.1 使用 TS 38.214 PDSCH Table 1

`beam_sls/mcs.py` 已按 TS 38.214 Table 5.1.3.1-1 显式记录每个
MCS index 的：

```text
modulation order Qm
target code rate R x 1024
code rate R
spectral efficiency Qm * R
```

不再根据频谱效率区间反推 `Qm` 和码率。MCS 10 现在固定为：

```text
Qm = 4
R x 1024 = 340
R = 0.33203125
η = 1.328125
```

独立使用的 `tbs_bits_from_mcs()` 也由原来的“信息比特数向下取 8 bit 整数倍”
近似，改为执行 TS 38.214 Sec. 5.1.3.2 的 TBS 量化、Table 5.1.3.2-1
小 TBS 查表和 code-block 分段流程。

### 2.2 正式 Sionna 路径不再由 `mcs.py` 解释 MCS

正式链路自适应路径现在统一由 Sionna NR 工具完成：

```text
MCS index
  -> sionna.phy.nr.decode_mcs_index()
  -> Qm / target coderate
  -> sionna.phy.nr.calculate_tb_size()
  -> TBS
  -> predicted rate / goodput
```

具体行为：

- ILLA 继续使用 Sionna SYS 选择 MCS；
- `predicted_rate_mbps` 使用 Sionna 解码出的 `Qm/R` 和标准 TBS；
- ACK 时的 `goodput_bits` 使用同一个 Sionna TBS；
- `goodput_mbps = goodput_bits / slot_duration`；
- `SchedulerLinkLookup` 的 MCS-to-rate 缓存来自正式 Sionna adapter，不再读取
  `mcs.py`。

修改后，`beam_sls/scheduler.py`、`beam_sls/feedback.py` 和
`beam_sls/link.py` 均不再导入 `mcs.py`。当前生产代码没有 `mcs.py`
依赖；该模块只保留为标准 PDSCH Table 1/TBS 独立工具，并被诊断脚本和回归测试
使用。

### 2.3 删除链路自适应 fallback

已删除：

```text
PrecomputedTableFallbackAdapter
fallback_precomputed_table
precomputed_table
mcs_surrogate
logistic_surrogate
bler_curve_slope
fallback_snr_min_db
fallback_snr_max_db
fallback_snr_step_db
```

Sionna SYS/ILLA 不可初始化时，仿真现在直接报告真实错误，不再自动切换到本地
MCS/BLER 表。`make_reports()`、`schedule()` 和 `run_tti_loop()` 要求调用方
显式传入 link-adaptation backend，防止正式路径因遗漏 adapter 而静默使用另一套
MCS 语义。

`sionna.fallback_to_numpy_if_unavailable` 仍只用于 TR 38.901 信道 backend；
它不会重新启用链路自适应 fallback。显式的
`scenario.channel_model: numpy_geometric_uma` 也仍可用于信道调试。

### 2.4 OLLA 只作用于调度器可见 SINR

本次传输 MCS 改为：

```text
mcs_selection_sinr_db
  = predicted_sinr_db - olla_offset_db

actual_mcs
  = ILLA(mcs_selection_sinr_db)

tbler
  = PHYAbstraction(effective_sinr_db, actual_mcs)
```

各字段含义：

- `predicted_sinr_db`：UE 上报信息和调度器干扰预测得到的调度器可见 SINR；
- `olla_offset_db`：每个 `(case_id, UE)` 的 OLLA backoff；
- `actual_mcs`：本 TTI 真正用于传输、TBLER 查询和 TBS 计算的 MCS；
- `effective_sinr_db`：真实 post-scheduling EESM SINR，只用于评估该 MCS 的
  TBLER/ACK；
- `mcs_selection_sinr_db`：本次选择 MCS 时实际使用的调度器侧 SINR。

offset 的符号规则保持不变：

```text
ACK:
  offset <- offset - olla_step_db

NACK:
  offset <- offset
            + olla_step_db * (1-target_bler) / target_bler
```

正 offset 表示 backoff，会降低选 MCS 时使用的 predicted SINR；offset 为负时，
减去负值等价于提高 predicted SINR。无论 offset 正负，都不会作用到
`effective_sinr_db`。

## 3. 更新后的使用方式

### 3.1 YAML 配置

正式下行 PDSCH 配置：

```yaml
system:
  target_bler: 0.1

link_abstraction:
  mode: sionna_sys_precomputed_bler
  mcs_table_index: 1
  mcs_category: 1
  olla_enabled: true
  olla_step_db: 0.1
  olla_warmup_tti: 50

sionna:
  fallback_to_numpy_if_unavailable: false
```

含义：

- `mcs_table_index: 1`：TS 38.214 PDSCH Table 1；
- `mcs_category: 1`：Sionna SYS PDSCH；
- link adaptation 必须成功创建 Sionna SYS backend；
- `fallback_to_numpy_if_unavailable` 仅控制信道 backend，推荐正式仿真设为
  `false`。

以下链路模式已经删除，不能再配置：

```yaml
link_abstraction:
  mode: fallback_precomputed_table
```

### 3.2 运行命令

缩小规模 smoke：

```bash
python -m beam_sls.run \
  --config configs/v2_one_site_three_sector.yaml \
  --out runs/v2_16_mcs_olla_smoke \
  --num-drops 1 \
  --num-tti 2 \
  --warmup-tti 1 \
  --skip-heatmap
```

七站点配置：

```bash
python -m beam_sls.run \
  --config configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml \
  --out runs/v2_16_7site21cell
```

运行环境必须安装与项目兼容的 Sionna SYS、Sionna PHY 和 TensorFlow。

## 4. 输出与检查方法

本轮无新增输出文件，也没有新增 CSV 字段。以下现有输出的数值语义发生修正。

### 4.1 `metrics/schedules.csv`

重点字段：

```text
predicted_sinr_db
predicted_mcs
predicted_rate_mbps
```

`predicted_rate_mbps` 现在与 `predicted_mcs` 使用同一张 Sionna PDSCH MCS 表
和标准 TBS。

### 4.2 `metrics/link_tti.csv`

重点字段：

```text
predicted_sinr_db
predicted_mcs
effective_sinr_db
olla_offset_db
mcs_selection_sinr_db
actual_mcs
tbler
ack
goodput_bits
goodput_mbps
```

应满足：

```text
OLLA enabled:
  mcs_selection_sinr_db
    = predicted_sinr_db - olla_offset_db

OLLA disabled:
  mcs_selection_sinr_db
    = predicted_sinr_db
```

`effective_sinr_db` 不要求等于 `mcs_selection_sinr_db`。两者差异反映调度器预测
与实际 post-scheduling 链路质量的差异，不应再通过重新选择 MCS 被消除。

ACK 时：

```text
goodput_bits = Sionna TS 38.214 TBS(actual_mcs)
```

NACK 时：

```text
goodput_bits = 0
```

## 5. 代表性数值

使用当前默认 PDSCH 资源配置：

```yaml
pdsch:
  num_prbs: 132
  num_symbols: 12
  dmrs_overhead_re_per_prb: 18
  num_layers_per_ue: 1
  slot_duration_ms: 0.125
```

MCS 10 的标准结果为：

```text
Qm = 4
R = 340/1024
η = 1.328125
TBS = 22032 bit
rate = 176.256 Mbps
```

该数值可用于检查 `predicted_rate_mbps` 和 ACK 后的 `goodput_bits` 是否仍存在
旧表错位。

## 6. 主要修改文件

- `beam_sls/mcs.py`
  - PDSCH Table 1；
  - TS 38.214 TBS；
  - 删除 surrogate SINR threshold 和 logistic BLER。
- `beam_sls/link_adaptation.py`
  - Sionna MCS decode/TBS；
  - 删除 link-adaptation fallback；
  - scheduler rate cache 改用正式 adapter。
- `beam_sls/feedback.py`
  - 删除本地 MCS/BLER fallback。
- `beam_sls/scheduler.py`
  - 删除本地 predicted-rate/MCS fallback。
- `beam_sls/link.py`
  - OLLA 改为作用于 `predicted_sinr_db`；
  - TBLER 仍使用真实 `effective_sinr_db`；
  - goodput 强制使用正式 adapter TBS。
- `beam_sls/config.py` 和 `configs/*.yaml`
  - 删除链路 fallback 参数；
  - 默认不再自动回退。
- `tests/test_mcs_standard.py`
  - MCS 10 标准语义；
  - 标准 TBS/rate；
  - Sionna adapter 调用链；
  - fallback 模式拒绝。
- `tests/test_smoke.py`
  - OLLA/MCS 因果关系回归。

## 7. 验证与兼容性

已完成：

```text
python -m compileall -q beam_sls tests
git diff --check
```

结果通过。

另外完成了最小手工回归，验证：

- MCS 10 为 `Qm=4, R=340/1024, η=1.328125`；
- 当前默认 PDSCH 资源下 TBS 为 `22032 bit`；
- rate 为 `176.256 Mbps`；
- 模拟高 `effective_sinr_db`、低 `predicted_sinr_db` 时，MCS 仍按低
  predicted SINR 选择；
- `effective_sinr_db` 只进入 TBLER；
- `fallback_precomputed_table` 会被拒绝。

当前执行环境未安装完整 pytest、PyYAML 和 Sionna 运行依赖，因此尚未在本机执行
完整 pytest 和真实 Sionna 端到端仿真。应在服务器 Sionna 环境中补充运行：

```bash
python -m pytest \
  tests/test_mcs_standard.py \
  tests/test_sionna_link_adapter.py \
  tests/test_smoke.py \
  tests/test_scheduler_v210.py \
  tests/test_pf_continuous_tti.py \
  -q --tb=short -x
```

并执行第 3.2 节的 smoke 命令检查实际 CSV。

兼容性注意事项：

- 正式仿真现在强制依赖 Sionna SYS，不再支持无 Sionna 的链路 fallback；
- 旧 YAML 中的 `fallback_precomputed_table` 和 fallback SNR 参数必须删除；
- `predicted_rate_mbps`、TBS 和 goodput 数值会相对旧版本发生变化，这是修复
  MCS 表语义后的预期结果；
- `actual_mcs` 不再跟随真实 `effective_sinr_db` 立即变化，而是通过
  ACK/NACK 驱动的 OLLA offset 逐 TTI 调整；
- 输出文件名和 CSV 字段保持兼容，但上述字段的物理含义以本说明为准。

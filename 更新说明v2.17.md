# 更新说明 v2.17：复用 MCS 决策表、批量 TBLER 与 PHY TBLER 预计算

## 1. 更新目的与原因

本轮更新用于降低连续 TTI 仿真中链路自适应和 PHY 抽象的重复计算开销。

初始化阶段原本已经生成：

```text
[init] scheduler link lookup ready, regions=27
```

该 `SchedulerLinkLookup` 已经使用正式 Sionna ILLA 扫描并细化 SINR 决策边界，
可以将任意调度器侧 SINR 映射为 MCS、outage 和 predicted rate。

旧的实际链路评估却没有复用该结果。每个 TTI、每个已调度 UE 都会分别执行：

```text
ILLA(predicted_sinr_db - olla_offset_db)
PHYAbstraction(effective_sinr_db, actual_mcs)
```

一个每 TTI 最多调度 6 个 UE 的场景，会产生约：

```text
6 次 ILLA 调用
6 次 PHYAbstraction 调用
```

这些调用大部分是 TensorFlow/Sionna 小张量调用，Python 与框架调度开销明显。
此外，非连续模式在固定 schedule 和固定信道下，会在每个 TTI 重复计算相同的
EESM effective SINR。

本轮将 MCS 选择与 TBLER 评估拆开：

```text
MCS 选择：
  predicted_sinr_db - OLLA offset
    -> SchedulerLinkLookup
    -> actual_mcs

TBLER 评估：
  effective_sinr_db + actual_mcs
    -> PHY TBLER lookup
    -> tbler
```

目标是保持正式 Sionna ILLA/PHY 语义，同时把 TTI 热循环中的框架调用改为
批量调用或纯 NumPy 查表。

## 2. actual MCS 复用 SchedulerLinkLookup

### 2.1 新的数据流

实际传输 MCS 现在按以下路径选择：

```text
OLLA enabled:
  mcs_selection_sinr_db
    = predicted_sinr_db - olla_offset_db

OLLA disabled:
  mcs_selection_sinr_db
    = predicted_sinr_db

actual_mcs
  = SchedulerLinkLookup(mcs_selection_sinr_db)
```

`effective_sinr_db` 仍然只用于已选 MCS 的 TBLER 评估，不参与 MCS 选择。
因此 v2.16 修正后的因果关系保持不变。

### 2.2 与正式 Sionna ILLA 对齐

`SchedulerLinkLookup` 的决策区域仍由正式 Sionna ILLA 在初始化阶段生成：

1. 在配置范围内进行密集 SINR 扫描；
2. 找出 MCS/outage 发生变化的区间；
3. 对每个决策边界进行二分细化；
4. 使用额外验证点与正式 Sionna 输出比较。

常规运行期只使用 NumPy `searchsorted`。对于：

- 超出 lookup SINR 范围的值；
- 距离决策边界小于 `boundary_guard_db` 的值；
- 非有限 SINR；

仍会批量回退正式 Sionna ILLA，以避免边界数值误差。actual MCS 的回退只调用
ILLA，不再为了取得 MCS 同时执行不需要的 PHY TBLER 计算。

### 2.3 批量 actual MCS 查询

同一 TTI 的全部：

```text
mcs_selection_sinr_db[scheduled UE]
```

现在一次传入 scheduler lookup。旧 adapter 或测试 adapter 没有批量接口时，
仍保留兼容的标量 fallback。

主要代码：

- `beam_sls/link_adaptation.py::SchedulerLinkLookup`
- `beam_sls/link.py::run_tti_loop`
- `beam_sls/sim.py::run_simulation`

## 3. 同一 TTI 批量计算 TBLER

`BaseLinkAdapter` 新增：

```text
tbler_from_sinr_db_batch(sinr_db, mcs_index)
```

`SionnaSYSAdapter` 对该接口提供正式批量实现。一个 TTI 中所有已调度 UE 的：

```text
effective_sinr_db[N]
actual_mcs[N]
num_allocated_re[N]
```

会一起传给一次 `PHYAbstraction` 调用。

当 PHY TBLER lookup 关闭时，运行期调用量由旧的：

```text
N 个已调度 UE -> N 次 PHYAbstraction([1 UE])
```

改为：

```text
N 个已调度 UE -> 1 次 PHYAbstraction([N UE])
```

批量结果继续执行与旧标量路径相同的处理：

- 非有限或负 TBLER 视为 `1.0`；
- 有效 TBLER 限制在 `[0, 1]`；
- ACK/NACK 仍按原随机数顺序逐链路抽样；
- TBS、goodput 和 OLLA 更新规则不变。

## 4. PHY TBLER 预计算表

### 4.1 构表方式

默认在初始化阶段构建：

```text
TBLER(effective SINR, MCS)
```

默认配置为：

```text
SINR: -40.0 dB 到 80.0 dB
步长: 0.1 dB
MCS:  0 到 28
```

因此表尺寸为：

```text
1201 SINR points × 29 MCS
  = 34,829 TBLER points
```

全部点由正式 Sionna `PHYAbstraction` 批量生成。默认
`build_batch_size=65536`，当前默认表可以在一个批次内完成；如果用户使用更细
网格，构建过程会自动分批，以限制峰值内存。

### 4.2 运行期插值

TTI 循环不再调用 Sionna PHY，而是按 actual MCS 分组并执行 NumPy 线性插值：

```text
tbler
  = np.interp(
      effective_sinr_db,
      sinr_grid_db,
      tbler_table[actual_mcs],
    )
```

超出配置范围的 SINR 使用表的端点值。查询使用的
`num_allocated_re` 与构表时不一致时，会自动回退正式批量 PHY 调用，避免错误
复用不同资源配置的 TBLER。

### 4.3 开关与精度

新增默认配置：

```yaml
link_abstraction:
  tbler_lookup:
    enabled: true
    sinr_min_db: -40.0
    sinr_max_db: 80.0
    sinr_step_db: 0.1
    build_batch_size: 65536
```

参数含义：

- `enabled`：是否构建和使用 TBLER 表；
- `sinr_min_db` / `sinr_max_db`：SINR 网格范围；
- `sinr_step_db`：插值网格间隔，越小越接近原始曲线，但初始化点数越多；
- `build_batch_size`：每次正式 Sionna PHY 构表调用的最大点数。

如果需要逐 TTI 使用正式 PHY 输出而不插值，可配置：

```yaml
link_abstraction:
  tbler_lookup:
    enabled: false
```

此时 actual MCS 仍使用 scheduler lookup，TBLER 则按每 TTI 一个批次调用正式
`PHYAbstraction`。

## 5. 其他热循环优化

在非连续 TTI 模式中，一个 `run_tti_loop()` 内的 schedule 和信道保持固定，
因此每条已调度链路的 EESM effective SINR 也固定。

本轮将：

```text
SINR grid -> EESM -> effective_sinr_db
```

移到 TTI 循环外，每条链路只计算一次。OLLA offset、actual MCS、TBLER、
ACK/NACK 和 goodput 仍按每个 TTI 更新。

连续 TTI 模式继续通过 `run_one_tti()` 使用当前演化后的信道，每个 TTI 会重新
计算真实 effective SINR，不会错误复用前一 TTI 的值。

## 6. 初始化状态与输出

本轮无新增 CSV/JSON 文件，也没有改变 `metrics/link_tti.csv` 的字段。

### 6.1 `link_abstraction_status.json`

新增：

```text
phy_tbler_lookup
```

其中记录：

```text
enabled
sinr_min_db
sinr_max_db
sinr_step_db
num_sinr_points
num_mcs
num_table_points
build_batch_size
build_elapsed_s
```

现有 `scheduler_lookup` 状态继续记录 MCS 决策区域、边界数量和构建耗时。

### 6.2 `metrics/runtime_phases.csv`

新增初始化阶段：

```text
phy_tbler_lookup_build
```

现有：

```text
scheduler_link_lookup_build
```

继续保留。两个阶段分别记录构建耗时和对应的 Sionna 调用计数，避免把 TBLER
构表调用错误归入 scheduler lookup 构建阶段。

运行期计数还可用于确认：

- `lookup_queries`：actual MCS 和 scheduler 的 lookup 数量；
- `lookup_boundary_fallback_items`：MCS 边界回退数量；
- `lookup_out_of_range_items`：MCS 范围外回退数量；
- `tbler_lookup_queries` / `tbler_lookup_items`：运行期 TBLER 查表量；
- `tbler_lookup_clipped_items`：使用 SINR 端点的数量；
- `phy_abstraction_invocations`：正式 PHY 实际调用次数。

## 7. 新增 1 site × 3 cell 配置

新增：

```text
configs/v2_12_1site3cell_persite_baseline_topk_50drop_200tti_10ue.yaml
```

该配置以：

```text
configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml
```

为基础，保留阵列、RF、feedback、evaluation、PF 和连续 TTI 设置，并调整为：

```yaml
topology:
  layout: one_site_three_sector
  num_sites: 1
  sectors_per_site: 3

ue_drop:
  num_ut_per_sector: 10

analysis:
  baseline_no_interference_upper_bound: false
```

每个 drop 的规模为：

```text
1 site
3 cells
10 UE/cell
30 UE/drop
```

总发射功率调整为：

```yaml
system:
  tx_power_dbm: 37.782
```

其含义是 6 个独立 TX resource 各约 30 dBm，即每个 TRP 合计约 33 dBm，与原
七站点配置的单 TRP/单 TX resource 功率保持一致。

配置同时显式启用 v2.17 TBLER lookup：

```yaml
link_abstraction:
  tbler_lookup:
    enabled: true
    sinr_min_db: -40.0
    sinr_max_db: 80.0
    sinr_step_db: 0.1
    build_batch_size: 65536
```

## 8. 更新后的使用方式

### 8.1 1 site × 3 cell 完整运行

```bash
python -m beam_sls.run \
  --config configs/v2_12_1site3cell_persite_baseline_topk_50drop_200tti_10ue.yaml \
  --out runs/v2_17_1site3cell_10ue
```

该配置执行：

```text
50 drops
50 warmup TTI/drop
200 measured TTI/drop
3 evaluation cases
```

其中 evaluation cases 为：

```text
baseline__greedy
topk_conflict_id__adaptive_lambda_greedy
topk_conflict_id__hard_conflict_greedy
```

不会生成 `baseline_no_interference_upper_bound` case。

### 8.2 建议先运行缩小规模 smoke

```bash
python -m beam_sls.run \
  --config configs/v2_12_1site3cell_persite_baseline_topk_50drop_200tti_10ue.yaml \
  --out runs/v2_17_1site3cell_smoke \
  --num-drops 1 \
  --num-tti 2 \
  --warmup-tti 1 \
  --skip-heatmap
```

启动时应看到：

```text
[init] scheduler link lookup ready, regions=...
[init] PHY TBLER lookup ready, shape=29x1201, elapsed=...
```

### 8.3 强制使用每 TTI 批量正式 PHY

在 YAML 中关闭：

```yaml
link_abstraction:
  tbler_lookup:
    enabled: false
```

然后使用相同命令运行。此模式适合与插值结果进行精度对比。

## 9. 主要修改文件

- `beam_sls/link_adaptation.py`
  - Sionna ILLA 批量接口；
  - Sionna PHY TBLER 批量接口；
  - actual-MCS 专用 scheduler lookup 查询；
  - `PHYTblerLookup` 构建和 NumPy 插值；
  - 标量/custom adapter 兼容路径。
- `beam_sls/link.py`
  - actual MCS 与 TBLER adapter 分离；
  - 同一 TTI 批量查询 MCS/TBLER；
  - 非连续模式复用 effective SINR。
- `beam_sls/sim.py`
  - 初始化并传递 scheduler lookup 和 PHY TBLER lookup；
  - 记录 lookup 状态、构建耗时和调用计数。
- `beam_sls/config.py`
  - 新增 `link_abstraction.tbler_lookup` 默认配置。
- `docs/yaml_parameter_reference.md`
  - 新增 TBLER lookup 参数说明。
- `docs/repository_memory.md`
  - 更新 actual MCS/TBLER 数据流与运行期调用约定。
- `tests/test_smoke.py`
  - 同 TTI MCS/TBLER 批量调用回归。
- `tests/test_scheduler_v210.py`
  - actual-MCS lookup fallback 不触发 PHY 的回归。
- `tests/test_sionna_link_adapter.py`
  - TBLER 构表、分批和插值回归。
- `configs/v2_12_1site3cell_persite_baseline_topk_50drop_200tti_10ue.yaml`
  - 新增 1 site × 3 cell、10 UE/cell 配置。

## 10. 验证结果

已完成：

```text
python -m compileall -q beam_sls tests
git diff --check
```

结果通过。

使用仓库内临时隔离依赖执行测试，结果为：

```text
79 passed
1 failed
```

通过的测试包括：

- 26 个除端到端首项外的 `test_smoke.py` 测试；
- 15 个 `test_scheduler_v210.py` 测试；
- 2 个 `test_sionna_link_adapter.py` 测试；
- 其余 36 个仓库回归测试。

新增的核心回归均通过：

- 同一 TTI 的 2 个 UE 只执行一次批量 actual-MCS 查询；
- 同一 TTI 的 2 个 UE 只执行一次批量 TBLER 查询；
- actual MCS 来自独立 scheduler lookup adapter；
- TBLER lookup 构建后，运行期插值不再调用底层 PHY；
- 线性测试曲线的插值结果与解析值一致；
- MCS lookup 在范围外/边界回退时不额外评估 TBLER；
- 原 OLLA warmup 和 predicted-SINR 因果关系保持不变。

目标 YAML 已通过加载和拓扑构建验证：

```text
sites = 1
cells = 3
UEs = 30
UEs per cell = 10
baseline_no_interference_upper_bound = false
```

剩余失败用例为：

```text
tests/test_smoke.py::test_smoke
```

该用例在进入本轮修改的链路评估路径之前失败：固定随机 drop 中的所有候选均被
测试用 fake adapter 判为 outage，scheduler 因而生成空 schedule，最终
`link_tti.csv` 没有数据行。其余 `test_smoke.py` 用例全部通过。

当前验证环境没有完整 Sionna/TensorFlow 运行依赖，因此
`test_sionna_link_adapter.py` 的正式 Sionna 初始化分支会按现有测试逻辑提前
返回。应在服务器 Sionna 环境补充：

```bash
python -m pytest \
  tests/test_sionna_link_adapter.py \
  tests/test_scheduler_v210.py \
  tests/test_smoke.py \
  tests/test_pf_continuous_tti.py \
  -q --tb=short -x
```

并执行第 8.2 节 smoke 命令，检查实际启动日志、
`link_abstraction_status.json` 和 `metrics/runtime_phases.csv`。

## 11. 兼容性与已知限制

- `link_abstraction.tbler_lookup` 有完整默认值，旧 YAML 不配置也会默认启用；
- 默认运行期 TBLER 从“正式 PHY 每 UE 标量调用”改为“Sionna 预计算表 +
  NumPy 线性插值”，数值可能产生与网格间隔对应的微小插值差异；
- 如需逐点正式 PHY 结果，可设置 `tbler_lookup.enabled: false`，此时仍保留
  每 TTI 批量调用优化；
- actual MCS 决策边界仍来自正式 ILLA，并保留边界/范围外正式 ILLA fallback；
- 默认 `-40～80 dB` 范围外使用 TBLER 表端点，应通过
  `tbler_lookup_clipped_items` 检查实际仿真是否频繁超界；
- 更小的 `sinr_step_db` 会增加初始化时间和表大小，但不会增加 TTI 循环中的
  Sionna 调用；
- TBLER 表与当前 MCS table、category、PDSCH allocated RE 和 Sionna BLER 表
  绑定；资源数不同的查询会回退正式批量 PHY；
- 本轮没有新增输出文件或 CSV 字段，但 `link_abstraction_status.json` 增加
  `phy_tbler_lookup`，`runtime_phases.csv` 增加
  `phy_tbler_lookup_build`；
- ACK 随机数顺序、OLLA 更新、TBS/goodput 计算、PF 更新和 CSV 字段语义保持
  兼容。

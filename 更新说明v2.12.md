# 更新说明 v2.12

本版本将默认毫米波发射模型改为“双极化共享波束 + TRP 共享码本池”。

- 一个物理面板只贡献一个并发波束资源，双极化不再分别计数。
- 每 TRP 最大并发波束数等于物理面板数，不再等于面板数乘极化数。
- 波束码字不再预绑定 `txru_index`；调度器可从该 TRP 的共享码本中选择任意
  `N` 个波束，再动态分配到空闲发射资源。
- 共享码本按单个物理面板的 `M×N` 空间维度定义。SLS 使用
  `measurement.tx_panel_index` 固定一个参考面板遍历候选波束；参考面板不构成
  TXRU 绑定。
- 完整 TRP 信道张量始终保留，不删除任何面板。默认启用
  `measurement.use_panel_channel_views`：SLS 从完整信道中提取参考面板的
  `M*N*P` 维临时计算子张量；实际传输则按调度波束动态分配到的物理面板提取
  对应部分。旧参数 `compact_tx_panel_channel` 仅作为兼容别名。
- Sionna CIR 的天线、极化、频率、路径和时间轴均显式解析。新增
  Sionna `PanelArray` 天线顺序到本项目码本顺序的确定性置换；维度不匹配时
  直接报错，不再通过重复、平均或截断来凑维度。
- 调度器对每个 TRP 独立执行容量约束，避免同一 TRP 超配。
- `max_mu_order: auto` 会按调度域内的 TRP 数扩展：sector 域、site 域和
  global 域分别统计各自包含的 TRP。
- `configs/v2_seven_site_hex.yaml` 明确配置为 7 站点、每站 3 个 TRP，
  全网共 21 个 TRP。

兼容旧实验时，可显式设置：

```yaml
rf_architecture:
  allow_independent_polarization_beams: true
```

此时仍按 panel-polarization/TXRU 子阵列生成独立码本和并发资源。

## 比例公平调度与连续 TTI 蒙特卡洛仿真

### 1. 更新目的与原因

旧流程虽然已经接受 `scheduler.objective: proportional_fair`，但一个 drop 内只在
开始时调度一次，后续全部 TTI 复用同一个 schedule 和同一个信道 `H`。PF 的历史
平均吞吐也要等整段 TTI 结束后才更新，因此不能反映“每个 TTI 根据瞬时速率和历史
吞吐重新竞争资源”的真实比例公平调度过程。

旧蒙特卡洛方式还容易把“每个 TTI 都重新实例化完整信道”与“一个 drop 内的连续
时变信道”混在一起。前者会重复生成 UE 位置、路损、阴影、簇和阵列信道，开销很大；
后者真正需要的是：

1. 每个 drop 只随机一次 UE 位置和大尺度信道；
2. 在该 drop 内连续运行固定时长；
3. 每个 TTI 仅按照 UE 速度引起的多普勒推进小尺度衰落；
4. 每个 TTI 重新测量、反馈、PF 调度、链路判决并更新 OLLA。

本轮增加了上述连续 TTI 模式。默认仍保持关闭，避免已有配置在升级后自动增加
每个 drop 的测量和调度次数。

### 2. 比例公平调度实现

#### 2.1 每 UE 状态

每个 evaluation case、每个 UE 维护一份历史平均实际 goodput：

```text
Tbar_i[t]
```

不需要再维护一个独立的静态“调度权值”。PF 权值由历史平均吞吐实时派生：

```text
w_i[t] = 1 / max(Tbar_i[t], epsilon)
```

对单个候选 UE：

```text
PF_metric_i[t] = predicted_rate_i[t] / Tbar_i[t]
```

对满足 MU order、TRP/panel 容量和冲突约束的共调度集合 `S`，调度器求：

```text
S*[t] = argmax_S sum(i in S) predicted_rate_i[t] / Tbar_i[t]
```

`predicted_rate` 仍然只使用调度器可见的 feedback 信息，不使用调度后的真实
effective SINR，避免未来信息泄漏。

#### 2.2 每 TTI 更新

每个 TTI 链路 ACK/NACK 判决完成后，用实际 goodput 更新 PF 平均吞吐：

```text
alpha = 1 / scheduler.pf_averaging_window_tti

Tbar_i[t+1]
  = (1-alpha)*Tbar_i[t] + alpha*actual_goodput_i[t]
```

未调度 UE 的本 TTI `actual_goodput_i[t]` 必须按 `0` 更新。否则未调度 UE 的
历史吞吐不会衰减，重新获得调度机会的速度会被低估。

PF 状态按 `case_id` 隔离，并在新 drop 开始时重置，避免新的随机 UE 位置继承上一
drop 同编号 UE 的吞吐历史。

主要代码：

- `beam_sls/scheduler.py::_pf_weight`
- `beam_sls/scheduler.py::update_pf_throughput`
- `beam_sls/sim.py::run_simulation`

### 3. OLLA 状态与更新

OLLA 与 PF 是两份不同状态：

- PF 保存每 UE 的历史平均实际 goodput；
- OLLA 保存每 `(case_id, UE)` 的 MCS SINR backoff。

实际 MCS 选择输入为：

```text
mcs_selection_sinr_db = effective_sinr_db - olla_offset_db
```

设目标 BLER 为 `p`，OLLA 基础步长为 `delta`：

```text
ACK:
  olla_offset_db <- olla_offset_db - delta

NACK:
  olla_offset_db <- olla_offset_db + delta*(1-p)/p
```

例如目标 BLER 为 10% 时，ACK 后减少 `delta`，NACK 后增加 `9*delta`。该不对称
更新使 OLLA 在实际 BLER 等于目标 BLER 时的期望变化为零。

OLLA 在同一个 drop 的 TTI 之间保持，在新 drop 开始时重置。`olla_warmup_tti`
期间同样逐 TTI 调度、抽样 ACK/NACK 并更新 OLLA，但 warmup 行不写入正式吞吐、
BLER 和 CDF 指标。

### 4. 连续 TTI 蒙特卡洛方式

#### 4.1 Drop 内固定与变化的量

每个 drop 只调用一次完整 topology/channel 生成，固定：

- UE 位置和 serving cell/site；
- 路损和阴影衰落；
- 基准信道的空间特征与时延/频率结构；
- 本 drop 使用的 UE 速度和载频。

每个 TTI 只推进每个 `(UE, TX unit)` 的复数小尺度状态。最大多普勒为：

```text
f_D = v*f_c/c
```

相邻 TTI 的 Jakes 相关系数为：

```text
rho = J0(2*pi*f_D*T_TTI)
```

小尺度状态使用一阶复 Gauss-Markov 更新：

```text
g[t+1] = rho*g[t] + sqrt(1-rho^2)*z[t]
```

其中 `z[t]` 是单位功率复高斯创新。当前实现为每个 `(UE, TX unit)` 维护一个标量
状态，再作用到该链路的完整 `H[FREQ, RX_AE, TX_AE]`，从而避免每 TTI 为完整
天线张量生成随机创新。这是低开销的 link-wise Jakes 近似，不是重新调用 Sionna
TR 38.901 生成新的簇。

UE 速度为 `0` 时 `rho=1`，信道在 drop 内保持不变。

#### 4.2 每 TTI 数据流

连续模式下，每个 TTI 依次执行：

```text
推进多普勒小尺度信道
  -> 重新计算 Gamma/服务波束测量
  -> 重新生成 UE feedback
  -> 使用当前 Tbar 做 PF 调度
  -> 合并全网 schedule
  -> 计算真实共调度干扰和 effective SINR
  -> OLLA 修正后选择 actual MCS
  -> TBLER 与 ACK/NACK
  -> 更新 OLLA
  -> 用实际 goodput 更新所有 UE 的 PF Tbar
```

该修改减少的是 topology 和完整信道实例化开销；每 TTI 的 Gamma 测量、feedback、
调度和真实链路评估仍然执行，因为这些量需要跟随瞬时小尺度信道和 PF 状态变化。

主要代码：

- `beam_sls/channel.py::DopplerChannelEvolver`
- `beam_sls/link.py::run_one_tti`
- `beam_sls/sim.py::run_simulation`

### 5. YAML 配置

以下示例在每个 drop 内正式统计连续 2 ms。默认 120 kHz SCS 配置中的 slot 时长为
0.125 ms，因此每个 drop 有：

```text
2.0 / 0.125 = 16 measured TTIs
```

```yaml
system:
  num_drops: 50
  target_bler: 0.1
  continuous_tti:
    enabled: true
    duration_ms: 2.0

pdsch:
  slot_duration_ms: 0.125

ue_drop:
  speed_kmh: 3.0

scheduler:
  objective: proportional_fair
  algorithm: greedy
  pf_tbar_init_mbps: 1.0
  pf_averaging_window_tti: 100

link_abstraction:
  olla_enabled: true
  olla_step_db: 0.1
  olla_warmup_tti: 100
```

约束和注意事项：

- `continuous_tti.duration_ms` 必须大于 0；
- `pdsch.slot_duration_ms` 必须大于 0；
- `duration_ms / slot_duration_ms` 必须是整数；
- 连续模式启用后，`duration_ms` 覆盖 `system.num_tti_per_drop`；
- `duration_ms` 只表示正式统计时长；如果配置了 `olla_warmup_tti`，每个 drop 还会
  额外运行相应数量的 warmup TTI；
- `pf_averaging_window_tti` 必须为正整数，数值越小，PF 对近期吞吐变化越敏感；
- `pf_tbar_init_mbps` 应为正数，避免初始权重过大或除零。

若需要旧行为：

```yaml
system:
  continuous_tti:
    enabled: false
```

此时继续使用 `system.num_tti_per_drop`，并保留旧的 drop 内 schedule/channel 复用
方式。

### 6. 参考 bash 命令

先把上面的连续 TTI 和 PF 配置加入所选 YAML，然后运行：

```bash
python -m beam_sls.run \
  --config configs/v2_one_site_three_sector.yaml \
  --out runs/v2_12_pf_continuous_2ms \
  --num-drops 50 \
  --objective proportional_fair \
  --algorithm greedy \
  --olla-warmup-tti 100 \
  --skip-heatmap
```

快速 smoke test：

```bash
python -m beam_sls.run \
  --config configs/v2_one_site_three_sector.yaml \
  --out runs/v2_12_pf_continuous_smoke \
  --num-drops 1 \
  --objective proportional_fair \
  --algorithm greedy \
  --olla-warmup-tti 2 \
  --skip-heatmap
```

连续模式下不要依赖 `--num-tti` 改变正式时长；正式 TTI 数由 YAML 中的
`continuous_tti.duration_ms / pdsch.slot_duration_ms` 决定。

### 7. 输出变化与解读

本轮没有新增独立输出文件，但以下现有文件的内容或粒度发生变化。

#### 7.1 `metrics/schedules.csv`

连续模式下每个正式 TTI 都写一条对应 case 的 schedule，并新增/使用 `tti` 字段。

重点字段：

- `drop`：随机 drop 编号；
- `tti`：该 drop 内正式统计 TTI 编号，从 0 开始；
- `scheme` / `case_id`：evaluation case；
- `feedback_scheme`：实际反馈方案；
- `algorithm`：调度算法；
- `objective_value`：本 TTI 的 PF 或 sum-rate 目标值；
- `num_scheduled`：本 TTI 调度 UE 数；
- `schedule_json`：UE、beam、预测 SINR/MCS/rate。

解读 PF 是否生效时，不应只看一个 TTI。应按 `(drop, case_id)` 排序后观察：

- 不同 TTI 的被调度 UE 是否轮换；
- 低历史吞吐 UE 是否逐渐获得更高调度优先级；
- `num_scheduled` 是否符合 RF capacity 和 MU order。

#### 7.2 `metrics/link_tti.csv`

该文件仍是每条已调度链路、每个正式 TTI 一行。连续模式下不同 TTI 使用不同的小尺度
信道和 schedule。

重点字段：

- `effective_sinr_db`：本 TTI 合并 schedule 后的真实 SINR；
- `predicted_mcs`：调度器预测 MCS；
- `actual_mcs`：OLLA 修正后实际使用的 MCS；
- `mcs_selection_sinr_db`：`effective_sinr_db - olla_offset_db`；
- `tbler`、`ack`：本次传输的 TBLER 和 ACK/NACK；
- `goodput_mbps`：ACK 后的实际 goodput；
- `olla_offset_db`：本 TTI 发送前的 OLLA backoff。

检查 OLLA 时，对固定 `(drop, case_id, ue_id)` 按 `tti` 排序：

- NACK 后下一次发送的 `olla_offset_db` 应增大；
- ACK 后下一次发送的 `olla_offset_db` 应减小；
- 长期 BLER 应向 `system.target_bler` 靠近。

#### 7.3 `metrics/ue_goodput.csv`

该文件对每个 `(drop, UE)` 跨全部正式 TTI 求平均，未调度 TTI 按 0 计入。

PF 公平性主要看：

- `avg_goodput_mbps` 的 UE 分布；
- `summary.csv/json` 中的 `p05_ue_goodput_mbps`；
- PF 与 `sum_rate` 的系统吞吐和 5% 边缘吞吐权衡。

#### 7.4 `metrics/drops.csv`

新增或明确记录：

- `continuous_tti_enabled`：是否启用连续模式；
- `continuous_duration_ms`：正式统计时长；
- `num_measured_tti`：换算后的正式 TTI 数；
- `olla_warmup_tti`：额外 warmup TTI 数；
- `ue_speed_kmh`：多普勒更新使用的 UE 速度。

首先检查这里，可以确认 YAML 中的 2 ms 是否实际解析成预期的 16 个 TTI。

#### 7.5 `metrics/scheduled_ue_su_throughput.csv`

连续模式下样本增加 `tti` 粒度，用于查看每个 TTI 被调度 UE 在所选服务 beam 上、
不加入共调度干扰时的 standalone SU throughput。该值不等于
`link_tti.csv` 中含真实 MU 干扰和 ACK/NACK 的 goodput。

### 8. 验证与兼容性

已完成：

- PF EWMA 定向测试：验证已调度 UE 按实际 goodput 更新，未调度 UE 按 0 衰减；
- 零速度测试：验证 `speed_kmh: 0` 时连续信道保持不变；
- 非零速度测试：验证多普勒状态会推进且不会修改调用方传入的原始信道存储；
- 缩小规模端到端连续仿真：0.25 ms / 0.125 ms 正确生成 2 个正式 TTI，
  `schedules.csv` 和 `link_tti.csv` 均包含对应 TTI；
- `python -m compileall -q beam_sls tests`；
- `git diff --check`。

新增测试文件：

```text
tests/test_pf_continuous_tti.py
```

兼容性：

- `system.continuous_tti.enabled` 默认是 `false`，已有 YAML 不配置时保持旧模式；
- `scheduler.objective: sum_rate` 仍可在连续模式下使用，此时每 TTI 重调度但不更新
  PF `Tbar`；
- OLLA 的符号约定和 ACK/NACK 更新公式保持不变；
- 连续模式增加每 TTI Gamma、feedback 和 scheduler 计算，整体耗时不会简单缩短为
  原来的 `1/TTI数`；优化的是重复 topology/完整信道实例化成本；
- 当前多普勒模型是低开销的 link-wise Jakes 近似，不是 Sionna 路径级 TIME 轴的
  精确演化。

由于当前可用 Python 环境缺少 `pytest`、`PyYAML` 和 `matplotlib`，本轮没有运行
完整 pytest 套件；已使用可用运行时完成定向函数测试、编译检查和缩小规模主流程
验证。

## 测量域、位置归属与每 Drop 单次测量上报

> 本节是 v2.12 的后续修订。若与前文“比例公平调度与连续 TTI 蒙特卡洛仿真”
> 中关于“每 TTI 重新测量、反馈和调度”的描述冲突，以本节为准。

### 1. 更新目的与原因

旧实现将 Gamma 测量范围隐式绑定到 `scheduler.domain_mode`，无法独立表达以下三种
测量范围：

1. UE 仅测量所在小区/TRP 的波束；
2. UE 测量所在小区所属站点全部小区的波束；
3. UE 测量所在六边形蜂窝各顶点上的小区波束，而不是把顶点站点的全部小区纳入。

旧 UE drop 还会在生成坐标时直接继承目标 sector 的 `serving_cell`。虽然默认规则
通常能让该标签与位置一致，但没有显式执行几何归属，也不能证明归属结果未使用
RSRP。

另外，连续 TTI 分支原先会在每个 TTI 重新计算 Gamma、生成 feedback 并重新调度。
本轮将生命周期修改为：每个 drop 只完成一次测量、上报和调度；后续 TTI 复用这些
结果，仅推进小尺度信道并执行真实链路评估、HARQ 风格 ACK/NACK 和 OLLA 更新。

### 2. 更新方式

#### 2.1 UE 撒点和位置归属

每个 cell 最终保留恰好 `ue_drop.num_ut_per_sector` 个 UE。归属过程只使用几何
信息：

```text
UE 坐标
  -> 选择欧氏距离最近的 site
  -> 在该 site 内选择方位角误差最小的 sector boresight
  -> 得到 serving_cell 和 site_id
```

不会使用 pathloss、阴影衰落、小尺度衰落、RSRP 或测量结果。撒点通过拒绝采样
保留真正归属于目标 cell 的坐标，因此输出 UE 数不会因为重新归属而在小区之间
失衡。

主要代码：

- `beam_sls/topology.py::serving_cell_from_position`
- `beam_sls/topology.py::drop_uniform_cell`
- `beam_sls/topology.py::make_topology`

#### 2.2 三种测量域

新增独立配置 `measurement.domain_mode`：

| 模式 | 测量范围 |
|---|---|
| `trp` | UE 几何归属的 serving cell/TRP 的全部波束 |
| `site` | serving cell 所属站点全部 cell 的波束 |
| `neighbor_area` | serving site 周围每个实际存在的一阶邻站只选择一个朝向 UE 的 cell，并测量这些 cell 的全部波束 |

`neighbor_area` 的六边形顶点按 serving site 的一阶等距邻站解析。每个顶点只贡献
一个 cell：在该顶点站点内选择 boresight 最朝向 UE 的 sector。不会把该站点全部
三个 sector 都加入测量域。

服务波束集合和测量波束集合已经拆开：

- `service_beam_indices_by_ue`：只包含 UE serving cell 的服务候选；
- `measured_beam_indices_by_ue`：由 `measurement.domain_mode` 决定，只用于
  Gamma 干扰维度和 conflict beam 上报。

因此 `site` 或 `neighbor_area` 测量不会让 UE 改由其他 cell 的波束提供服务。
稀疏 Gamma 块也由原来的单一方形集合改为：

```text
Gamma[UE, serving-cell service beam, measured interferer beam]
```

即每个 UE 可保存非方形的
`B_service × B_measurement` 局部块，同时继续支持全局 beam index 访问。

当前 `seven_site_hex` 是有限 7 站点站群，不做 wrap-around：

- 中心站有完整 6 个顶点；
- 外圈站只使用拓扑中实际存在的一阶邻站，因此可能少于 6 个顶点；
- 单站点拓扑没有邻站，配置 `neighbor_area` 会直接报错，不会静默生成空 report。

主要代码：

- `beam_sls/topology.py::normalize_measurement_domain`
- `beam_sls/topology.py::neighbor_area_vertex_sites`
- `beam_sls/topology.py::measurement_cell_ids_by_ue`
- `beam_sls/sim.py::_beam_indices_by_ue_cells`
- `beam_sls/measurement.py::SparseGamma`
- `beam_sls/measurement.py::compute_gamma_measurement`
- `beam_sls/feedback.py::make_reports`

#### 2.3 测量域与调度域完全独立

测量域和调度域可自由组合，不再进行组合限制：

| 配置 | 独立职责 |
|---|---|
| `measurement.domain_mode` | 决定 UE 测量和上报哪些干扰波束 |
| `scheduler.domain_mode` | 决定哪些 UE/report 进入同一个调度问题，以及对哪些共调度波束进行干扰预测 |

UE 的服务候选始终来自其 serving cell，因此 `neighbor_area + per_site_joint`、
`site + per_sector_independent` 等组合不会导致独立调度域占用其他域的 TX 资源。

如果调度域中出现 UE 未测量的共调度干扰波束，调度预测按
`scheduler.unknown_interference_policy` 处理。当前支持：

```yaml
scheduler:
  unknown_interference_policy: zero
```

`zero` 表示调度器无法从 report 得知该干扰时，在预测中按 0 处理。调度后的真实
链路评估不受此策略影响，仍根据合并后的全网实际发射波束和真实信道计算干扰、
effective SINR、BLER 与 ACK/NACK。

#### 2.4 每个 Drop 一次测量上报

当前 drop/TTI 数据流为：

```text
每个 drop:
  topology + 几何 UE 归属
  -> 生成一次基准信道
  -> 按 measurement.domain_mode 计算一次 Gamma
  -> 每种 feedback scheme 生成一次 UE report
  -> 每个 evaluation case 调度一次
  -> 后续 warmup/正式 TTI:
       推进小尺度信道
       -> 复用 measurement/report/schedule
       -> 真实链路评估
       -> TBLER 与 ACK/NACK
       -> 更新 OLLA
```

后续 TTI 不再重新测量、重新上报或重新调度。`schedules.csv` 仍保留每个正式 TTI
一行，便于与 `link_tti.csv` 对齐，但这些行记录的是同一个 drop schedule 的复用
结果。

### 3. 更新后如何使用

#### 3.1 YAML 配置

TRP 域：

```yaml
measurement:
  domain_mode: trp

scheduler:
  domain_mode: per_sector_independent
```

站点域：

```yaml
measurement:
  domain_mode: site

scheduler:
  domain_mode: per_site_joint
```

邻区域：

```yaml
topology:
  layout: seven_site_hex
  num_sites: 7
  sectors_per_site: 3

measurement:
  domain_mode: neighbor_area

scheduler:
  # 可独立选择 per_sector_independent、per_site_joint 或 global。
  domain_mode: per_site_joint
```

每 cell 的 UE 数仍通过下列字段配置：

```yaml
ue_drop:
  num_ut_per_sector: 10
  distribution: uniform_in_sector
```

#### 3.2 参考 bash 命令

默认站点域配置：

```bash
python -m beam_sls.run \
  --config configs/v2_one_site_three_sector.yaml \
  --out runs/v2_12_measurement_site \
  --num-drops 1 \
  --num-tti 2 \
  --skip-heatmap
```

七站点邻区域需要先在 `configs/v2_seven_site_hex.yaml` 中设置：

```yaml
measurement:
  domain_mode: neighbor_area

scheduler:
  domain_mode: per_site_joint
```

然后运行：

```bash
python -m beam_sls.run \
  --config configs/v2_seven_site_hex.yaml \
  --out runs/v2_12_measurement_neighbor_area \
  --num-drops 1 \
  --num-tti 2 \
  --skip-heatmap
```

### 4. 输出变化与解读

#### 4.1 新增 `metrics/measurement_domains.csv`

生成条件：每次正常仿真都会生成。统计粒度为每个 `(drop, UE)` 一行。

字段：

- `drop`：drop 编号；
- `ue_id`：UE 编号；
- `measurement_domain_mode`：归一化后的测量域；
- `serving_cell`：按位置确定的归属 cell；
- `site_id`：按位置确定的归属 site；
- `service_cell_ids`：服务候选所属 cell，当前为 `[serving_cell]`；
- `measured_cell_ids`：该 UE 实际测量的 cell ID JSON 数组；
- `num_service_beams`：serving cell 中的服务候选波束数；
- `num_measured_cells`：实际测量 cell 数；
- `num_measured_beams`：测量域 cell 对应的波束数；
- `num_reported_beams`：服务波束与测量域波束去重合并后的总上报波束数。

建议验证方式：

- `trp`：每行 `measured_cell_ids` 应仅包含 `serving_cell`；
- `site`：所有 `measured_cell_ids` 应具有相同 `site_id`；
- `neighbor_area`：中心六边形应有 6 个 measured cells，且 6 个 cell 分别属于
  6 个不同的一阶邻站，而不是每个站点贡献 3 个 cell。

#### 4.2 现有输出变化

- `metrics/drops.csv` 新增 `measurement_domain_mode`，并记录
  `avg_measurement_beams_per_ue` 与 `avg_reported_beams_per_ue`；
- `metrics/reports.csv` 每个 `(drop, scheme, UE)` 仍只记录一次 report，不增加
  TTI 粒度；`report_json.measured_interference_beams` 记录 UE 实际上报的干扰
  波束 ID（serving-cell 服务波束与测量域波束的并集），`candidates` 只包含
  serving-cell 服务波束；
- `metrics/schedules.csv` 在连续模式中仍按 TTI 输出，但同一 drop/case 的
  schedule 被复用；
- scheduler metadata 记录 `measurement_reports_per_drop: 1`、
  `num_scheduler_invocations: 1` 和 `num_tti_schedule_reused`；
- `metrics/link_tti.csv` 仍按实际 TTI 记录变化的信道、SINR、TBLER、ACK/NACK、
  goodput 和 OLLA。

### 5. 验证与兼容性

新增测试：

```text
tests/test_measurement_domain.py
tests/test_pf_continuous_tti.py
```

并扩展：

```text
tests/test_smoke.py
```

覆盖内容：

- 多随机种子下，每个 cell 的最终 UE 数严格等于配置值；
- 每个 UE 输出归属与独立复算的“最近 site + 最近 sector boresight”一致；
- `trp` 只返回 serving cell；
- `site` 返回 serving site 的全部三个 cell；
- 七站点中心六边形返回 6 个邻区域顶点；
- 每个邻区域顶点只贡献一个 boresight 最朝向 UE 的 cell；
- 未知测量域会报错；
- 服务候选集合与测量干扰集合相互独立；
- 非方形稀疏 Gamma 能访问 serving-cell 服务波束对测量域干扰波束的条目；
- 非方形稀疏 Gamma 与全量 dense Gamma 的对应条目一致；
- 所有测量域均可与 sector/site/global 调度组合；
- full-Gamma 调度对未测量干扰使用 `unknown_interference_policy: zero`；
- 连续 TTI 中每个 drop 仅调用一次 Gamma 测量、feedback 生成和 case 调度；
- `reports.csv` 只保留 drop 粒度，`schedules.csv` 保持正式 TTI 粒度。

本轮实际完成：

- `python -m compileall -q beam_sls tests` 通过；
- `git diff --check` 通过；
- 七站点中心邻区域几何检查通过，得到 6 个不同顶点 cell；
- 20 个随机种子的七站点撒点回归通过，每个 cell 均严格保留配置数量的 UE，
  且全部 UE 的几何归属复算一致。

当前运行环境缺少 `pytest`、`PyYAML` 和 `matplotlib`，因此没有运行完整 pytest
和完整端到端仿真。相关 pytest 用例已经加入，依赖齐备后建议执行：

```bash
python -m pytest tests/test_measurement_domain.py \
  tests/test_pf_continuous_tti.py \
  tests/test_smoke.py \
  -q --tb=short -x
```

兼容性说明：

- 默认 `measurement.domain_mode` 为 `site`，与默认
  `scheduler.domain_mode: per_site_joint` 的旧有效干扰测量范围一致；
- 服务候选现在固定在几何 serving cell；旧实现中 site 测量域可能把同站其他
  cell 的波束也作为服务候选，本轮按“测量域只决定上报范围”的语义取消该行为；
- `configs/v2_one_site_three_sector.yaml` 和 `configs/v2_seven_site_hex.yaml`
  已显式设置 `measurement.domain_mode: site`；
- 测量域与调度域可以自由组合，不再要求 `neighbor_area` 必须使用 global 调度；
- UE 位置随机流仍由原 drop RNG 控制，但拒绝采样会改变后续随机数消耗，因此相同
  seed 与旧版本不保证得到逐坐标完全相同的 UE realization；
- `neighbor_area` 在有限站群边界不补虚拟顶点；需要所有 UE 都具有完整六顶点时，
  后续仍需实现更大站群或 wrap-around；
- 连续模式不再执行每 TTI PF 重调度。若实验目标是研究随 TTI 更新的 PF 资源轮换，
  需要另行提供“周期性重新测量/重新调度”配置，不能把当前 HARQ-only TTI 流程
  解释为动态 PF 调度。
## 静态调度簇统一测量域与调度域（最终设计）

> 本节是 v2.12 的最终设计，替代本文前面“测量域与调度域完全独立”以及
> `trp/site/neighbor_area` 独立测量域的说明。

### 1. 更新目的与原因

为保证每个 TX/波束/时频资源只有一个调度器拥有，调度簇改为对全网 cell 的
静态、完整、不交叠划分。每个 UE 只属于 serving cell 所在的一个簇：

```text
测量域 = 调度域 = UE 所属静态调度簇
```

这样不存在 UE 无簇归属、同时属于多个簇或两个调度器重复占用同一 TX 的情况。
代价是簇边缘 UE 不测量簇外波束，调度预测可能偏乐观；最终链路评估仍使用
合并后的全网真实发射计算干扰，不会在性能统计中删除簇外物理干扰。

### 2. 更新方式

#### 2.1 最大平均 RSRP 小区关联

对 UE `u`、cell `c`、该 cell 的波束集合 `B_c`、UE RX 波束集合 `Q_u` 和测量
频点集合 `F`，定义：

$$
\overline{P}^{\mathrm{RSRP}}_{u,c}
=
\max_{b\in\mathcal B_c,\ q\in\mathcal Q_u}
\left[
P_{\mathrm{TX}}\frac{1}{|\mathcal F|}
\sum_{f\in\mathcal F}
\left|q^{H}H_{u,\mathrm{tx}(b)}(f)w_b\right|^2
\right].
$$

关联结果为：

$$
c_u^\star=\arg\max_c\overline{P}^{\mathrm{RSRP}}_{u,c}.
$$

若多个 cell 完全相同，选择较小 `cell_id`。NumPy 几何信道同时修正为每个
`(UE, site)` 独立计算路损和阴影衰落，同站扇区复用同一大尺度状态，避免旧版
把 provisional serving-site 路损错误套到所有站点。

#### 2.2 静态簇与唯一归属

`beam_sls/topology.py` 新增：

- `resolve_static_scheduling_clusters`：构造并校验完整、不交叠的 cell 分区；
- `assign_ues_to_scheduling_clusters`：UE 继承 serving cell 的唯一 cluster；
- `cluster_cell_ids_by_ue`：将该 cluster 的全部 cell 作为测量和调度范围。

`UEReport` 新增 `scheduling_cluster`。调度器按该字段分组，每个簇只使用本簇
UE 上报的信息联合调度本簇服务波束。服务候选仍只来自 UE 的 serving cell。

### 3. 配置与运行

预定义划分：

```yaml
scheduler:
  cluster_mode: per_site  # per_cell | per_site | global | custom
```

自定义划分：

```yaml
scheduler:
  cluster_mode: custom
  static_clusters:
    - cluster_id: 0
      cell_ids: [0, 1, 2]
    - cluster_id: 1
      cell_ids: [3, 4, 5]
```

`custom` 中空簇、未知 cell、重复 cell 或未覆盖 cell 都会启动失败。旧
`scheduler.domain_mode` 仅在 `cluster_mode` 未配置时兼容映射；
`measurement.domain_mode` 不再参与仿真。

```bash
python -m beam_sls.run \
  --config configs/v2_seven_site_hex.yaml \
  --out runs/v2_12_static_clusters \
  --cluster-mode per_site \
  --num-drops 1 \
  --num-tti 2 \
  --skip-heatmap
```

### 4. 输出变化

`metrics/measurement_domains.csv` 保持 `(drop, UE)` 粒度，新增：

- `scheduling_cluster`：UE 唯一调度簇；
- `cluster_mode`：静态簇划分方式；
- `serving_average_rsrp_w`：最终 serving cell 的宽带平均 RSRP。

其中 `measurement_domain_mode` 固定为 `scheduling_cluster`。
`metrics/drops.csv` 新增 `scheduling_cluster_mode` 和
`num_scheduling_clusters`。`metrics/reports.csv` 的 report JSON 新增
`scheduling_cluster`。

### 5. 验证与兼容性

新增测试覆盖平均 RSRP 关联、确定性平局、custom 簇完整覆盖、重复 cell 拒绝、
漏配 cell 拒绝、UE 唯一归簇和簇 cell 测量范围。保留旧
`scheduler.domain_mode` 的兼容映射，但独立 `trp/site/neighbor_area` 测量模式
不再生效。完整 pytest 仍取决于环境中的 pytest、PyYAML 和绘图库依赖。

### 6. 静态簇模式与阶段耗时输出

四种静态模式定义如下：

- `per_cell`：每个 cell 单独形成一个簇；
- `per_site`：相同 `site_id` 下的全部 cell 形成一个簇；
- `global`：全网全部 cell 形成唯一簇；
- `custom`：严格采用 `scheduler.static_clusters` 中的 cell 分组。

预计主要耗时来自信道生成、全 cell 平均 RSRP 波束扫描、Gamma 测量、反馈链路
自适应、组合调度和多 TTI 真实链路评估。程序现在在每个阶段完成后输出
`elapsed=<seconds>s`，并将以下 phase 写入 `metrics/runtime_phases.csv`：

```text
topology_generation
channel_generation
average_rsrp_association
static_cluster_preparation
gamma_measurement
feedback_generation
scheduler
link_evaluation
```

前六项为 `(drop, all)` 粒度；后两项为 `(drop, case_id)` 粒度。CSV 的
`elapsed_s` 单位为秒。`channel_generation`、`gamma_measurement` 同时记录
实际 backend；`static_cluster_preparation` 记录 `cluster_mode` 和簇数量。

### 7. 七站点 per-site 双方案配置运行命令

配置文件：

```text
configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml
```

直接运行：

```bash
python -m beam_sls.run \
  --config configs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti.yaml \
  --out runs/v2_12_7site21cell_persite_baseline_topk_50drop_200tti
```

该命令直接采用 YAML 中的 50 drops、每 drop 200 个正式统计 TTI、50 个 warmup
TTI、`per_site` 静态簇、`baseline + greedy` 与
`topk_conflict_id + hard_conflict_greedy` 两个 evaluation case。拓扑图输出到
`figures/topology.png`，coverage heatmap 保持关闭。

## 每 TTI PF 重调度、可配置 TTI 数与系统吞吐 CDF（最终补充）

> 本节替代前文“连续模式每 drop 只调度一次、后续复用 schedule”的说明。
> 测量和 feedback 仍为每 drop 一次，但 warmup 和正式 TTI 都重新执行调度。

### 1. 更新目的与原因

连续模式需要同时满足：

- 固定一个 drop 的 UE 位置和大尺度信道，避免重复实例化完整信道；
- 每个 TTI 按多普勒推进小尺度信道；
- 每个 TTI 使用最新 PF 历史吞吐重新选择 UE/波束；
- warmup 同时让 PF 和 OLLA 状态收敛，但不污染正式统计；
- 系统吞吐 CDF 必须保留“调度为空或全部 NACK”的零吞吐 TTI。

旧聚合仅从存在链路行的 TTI 计算系统平均值，会遗漏完全为零的 TTI。本轮新增显式
的 `(scheme, drop, TTI)` 完整网格，修复该幸存者偏差。

### 2. 更新方式

连续模式每个 drop 只生成一次 Gamma 测量和 feedback report；随后每个 warmup 和
正式 TTI 执行：

```text
推进小尺度信道
  -> 使用当前 Tbar 重新调度 UE/波束
  -> 真实链路评估与 actual MCS
  -> ACK/NACK 和 OLLA 更新
  -> 更新所有 UE 的 PF Tbar（未调度 UE 按 0）
```

warmup TTI 不写入正式吞吐、BLER、schedule CDF 数据；正式统计区间内的全部 TTI
都会进入系统和 UE 吞吐统计。

### 3. YAML 与 bash 用法

可以直接按 TTI 数设置：

```yaml
system:
  continuous_tti:
    enabled: true
    # 非 null 时优先于 duration_ms。
    num_tti: 16
    warmup_tti: 300
    duration_ms: 2.0

pdsch:
  slot_duration_ms: 0.125

scheduler:
  objective: proportional_fair
  pf_averaging_window_tti: 100

link_abstraction:
  olla_enabled: true
  olla_step_db: 0.1
```

也可以令 `num_tti: null`，由 `duration_ms / slot_duration_ms` 换算正式 TTI 数。
`warmup_tti: null` 时沿用 `link_abstraction.olla_warmup_tti`。

CLI 可直接覆盖：

```bash
python -m beam_sls.run \
  --config configs/v2_one_site_three_sector.yaml \
  --out runs/v2_12_pf_cdf \
  --num-drops 100 \
  --num-tti 16 \
  --warmup-tti 300 \
  --objective proportional_fair \
  --algorithm greedy \
  --skip-heatmap
```

旧参数 `--olla-warmup-tti` 保留为 `--warmup-tti` 的兼容别名。

### 4. 新增输出与解读

#### 4.1 `metrics/system_tti_goodput.csv`

每个 `(scheme, drop, tti)` 一行：

- `system_goodput_mbps` 是该 TTI 所有已调度 UE 实际 goodput 之和；
- 完全没有成功传输的 TTI 也保留，并记录为 0；
- 对应图为 `figures/system_tti_goodput_cdf.png`；
- 该 CDF 反映瞬时系统吞吐及 ACK/NACK、小尺度信道和调度波动。

#### 4.2 `metrics/system_drop_avg_goodput.csv`

每个 `(scheme, drop)` 一行：

```text
avg_system_goodput_mbps
  = sum(该 drop 全部正式 TTI 的系统 goodput) / num_measured_tti
```

- 对应图为 `figures/system_drop_avg_goodput_cdf.png`；
- 该 CDF 反映不同 UE 位置和大尺度信道 realization 下的短时平均系统吞吐；
- `num_measured_tti` 用于核对每个 drop 的统计窗口长度。

#### 4.3 单 UE 吞吐 CDF

继续使用：

```text
metrics/ue_goodput.csv
figures/ue_goodput_cdf.png
```

每个 `(drop, UE)` 对全部正式 TTI 求平均；未调度或 NACK 的 TTI 按 0 计入。
这是 PF 公平性和 5% 边缘 UE 吞吐的主要统计口径。

`metrics/summary.csv/json` 的 `avg_system_goodput_mbps` 现在也使用完整 TTI 网格，
因此包含零吞吐 TTI。

### 5. 验证与兼容性

已验证：

- 系统 TTI 聚合会显式补齐零吞吐 TTI；
- 每 drop 平均值使用完整正式统计窗口；
- summary 的系统平均值包含零 TTI；
- `num_tti` 优先于 `duration_ms`；
- `warmup_tti` 可独立设置；
- 1 warmup + 2 measured TTI 的缩小端到端仿真通过；
- scheduler metadata 记录 3 次调度调用，且 `num_tti_schedule_reused=0`；
- `python -m compileall -q beam_sls tests` 与 `git diff --check` 通过。

兼容性：

- `continuous_tti.num_tti: null` 保留原 `duration_ms` 换算方式；
- `continuous_tti.warmup_tti: null` 保留原 `olla_warmup_tti`；
- 非连续模式继续使用 `system.num_tti_per_drop`；
- `--num-tti` 现在可覆盖连续模式的正式 TTI 总数；
- 测量和 feedback 仍每 drop 一次，避免每 TTI 重算 Gamma；调度则每 TTI 执行。
